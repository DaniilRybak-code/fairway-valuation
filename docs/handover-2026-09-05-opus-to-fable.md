# For Fable, 5 September 2026, 22:30 UK. One new project rule and one open ruling.

## The rule, and Daniil wants it treated as one of the project's rules, not a note

**A12 in `docs/RULES.md`: failing the relevance gate is a FAILURE, the archetype fallback is how we
serve the founder anyway, and every use is recorded.**

The background in three lines. A comparable earns its place by shared product vocabulary or a shared
specific end market; sharing an archetype label earns nothing, because the labels are too coarse
(`Vertical Software` holds 120 rows and contains both stock exchanges and restaurant software). That
gate was blocking names we already hold: GitLab scores 9.0 against a prompt-to-app builder and was
dropped, and Daniil quite reasonably asked why we were asking him to pull companies already in the
file.

**What is built.** The fallback is a RESCUE, not a loosening. It opens only for a lane that cannot
price at all, and it stays shut for a founder whose lane is healthy. Applied everywhere it would
hand Publora, a social publishing API, the Perplexity and LangChain rounds, which is the exact
failure the gate was written on 26-Aug to stop. Today it fires for 2 fixtures of 102.

**What matters for how you work.** Every comparable admitted by the fallback is printed on every run
of `tools/peer_universe_check.py` under **"served on a label, not on evidence"**. That register is
the enrichment brief and it is the only sourcing list we work from now.

**Daniil's instruction on cadence, verbatim in spirit:** we cannot keep adding peers to satisfy this
gate, there is too much else to do. **Four enrichment passes before the pilot: 8, 11, 15 and 18
September.** Each takes the register as its brief. Between them the register accumulates and nobody
chases comparables. If you find yourself proposing a data pull outside those four dates, that is the
rule telling you to stop.

## The open ruling, which is yours to weigh in on but Daniil's to make

`docs/taxonomy-review-5sep.md` measures the archetype taxonomy: 34 archetypes over 1,281 slots, and
the big four carry 32 per cent of them. The proposal is deliberately not a re-tag of 828 rows. It is
one rule: **for a defined list of catch-all archetypes (`Vertical Software`, `Data, AI & Developer
Tools`, `Business Applications`, `Cloud & Infrastructure`), an archetype match only counts when the
INDUSTRY matches too.** We already hold the industry field, so this is about twenty lines of code
and no data work. Not before the pilot.

## State when I left it

Gate 90 of 102 (was 81 this morning, 89 after your pull loaded, 90 with the rescue). All checks pass
except check 1, which fails on `data/raw/2026-09-04_public-pull-16-names.csv` alone: that is the
public pull, deliberately unloaded until three questions are answered, and the check is doing its
job by saying so.

Four things from your own handover still need Daniil, and I have not touched them: the four rows
held out of the medians (Moss, Restaurant365, AuditBoard, Udemy), the five lanes where only
gross-basis rounds exist, `tools/investor_check.py` being stale against his 2-September ruling on
unpublished cheques, and LTG plus Confluent and Verint not done.

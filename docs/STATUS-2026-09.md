# FAIRWAY STATUS — September 2026

**The one working document.** It replaces the tracker and the merged roadmap, both of which are
archived (see the foot). Convention, set by Daniil on 4 September: this document runs until
30 September, updated in place — every update stamped with date and time on the UK clock. On
1 October a fresh one starts from scratch (`Fairway_STATUS_2026-10.md`) and this one is stored.
An item moves from TO DO to DONE with a date the same day it finishes; the body never contradicts
itself; "today/tomorrow" never appears without a date.

**Created: 4 September 2026, 19:35 UK (Fable). Last updated: 4 September 2026, 20:02 UK (Opus; every stamp read off the clock).**

**For any new chat in this project:** this is the single source of truth on progress and to-dos.
Find it as `claude/Fairway_STATUS_2026-09.md` in the project docs (or search the project for
"status document"). Read it BEFORE reading any handover or archived tracker/roadmap.

**Pilot launches the week of 21 September 2026.** Rules of the product live in `docs/RULES.md`
in the repo; the data-durability protocol and standing rules from the old tracker are preserved
in the archived copy and remain binding.

---

# WHAT HAS BEEN DONE

## (i) Yesterday and today — 3 and 4 September

**The 100-fixture gate: PASSED.** The march went 43 → 102 real fixtures in one evening (3 Sep),
under Daniil's re-framed gate: peer-universe depth and breadth, no revenue figures, companies
taken randomly from YC and Product Hunt. 90 of 102 pass; 89 of 99 in-market; 26 archetypes now
exercised (was 18); three hard-tech names kept and marked OUT_OF_MARKET so the score reads both
ways. Audited by Fable on 4 Sep: all twelve checks pass, golden 0/102, the five least-trusted
tags verified against the companies' own pages, and the thin-lane diagnosis proven honest by a
planted comparable that surfaced at rank 1. *One remnant: the march commit is still only on
Daniil's laptop, and tonight's build sits beside it. The push is the first item below.*

**Landing v9.1, Daniil's 4-Sep page-2 markup (Fable, 4 Sep, 19:50 UK; in the working tree, not yet
pushed).** The Northsteer card reads in $ (illustrative figures swapped 1:1; the three UK funds' published
£ cheques converted at 1.35 $/£, Trading Economics 4 Sep, and the table says so). The four $0.78m cells
and the $0.64m cell carry a pop-up with the arithmetic, the same white card the peers use and the same
sum `app-growth.js` does: $50k a month, 60% plan, 4.0% a month, twelve months added. Every bar's hover is
now "$x of your metric times the range: $a to $b" instead of repeating the multiple's source; all seven
reconcile. Regression hover explains R² in plain words and how 74% is read off the line a tenth either
side; DCF hover shows the build-up (5.27% 30-year Treasury, FRED 2 Sep; beta 2.0; 6% MRP; cost of debt
22%; debt 15%; no tax shield) landing on 18.0%, Daniil's ruling being to hold 18% by choosing the inputs
until A1. Chip 5 aligned with the rail (the reviewer row's 14px negative margin had pushed it right).
Our read: serif titles outside the boxes, concerns under advocacy, likely-investors table on the right,
"who invests in the space" line gone. Rendered headless at --exp 1 and hovered; both files written to the
laptop byte-identical to the container copies.

**The eight-figure data audit closed** (Fable 4 Sep: six figures hold — Dream Sports, Atom Bank,
Oura, Discord, Konfio, Tala; two ruled) **and the verdicts are applied in the data** (Opus,
3 Sep, commit e9ab546): Klarna restated at pricing-date ECB rates with the entity named
($1,208.8m / 37.7x Jun-21, $1,298.8m / 5.16x Jul-22, Klarna Bank AB); LEAD School on printed
revenue-from-operations at 142.17x; Starling and Upgrade flagged Pre-money; Atom Bank tagged
pre/post-unspecified; Oura carries its source-upgrade note.

**Recommendations Day 3 closed (Opus, 4 Sep evening).** The last promised feature with no engine
behind it now has one. `unit_economics` reads gross margin against the peer set and, when the
founder's margin sits more than 15 points from the group median, says the thing nothing was saying:
their field is already drawn on gross profit rather than revenue. `market_position` prices the
founder's own lane against their whole family and names what the DIRECT / ADJACENT / BROAD label
costs, which is the first time a founder is told that a BROAD set prices nothing. Both refuse to
speak without a distribution behind them. The renderer is `recommendations.js` on the investors.js
contract: a whitelist (`BLOCK_FIELDS`), three sentences a card, ordered by turns of revenue moved,
and nothing rendered at all when there is nothing to say. **Check 13 is the acceptance test written
down**: every number printed to a founder must be declared by the dimension that printed it, and
every declared figure is recomputed from the values it claims to come from, so a median is re-taken
and a percentile recounted rather than trusted. All 13 checks pass; golden 0/102.

**Four rulings applied and pushed the same evening (Opus, 4 Sep, 20:02 UK).** The march batch
went up as `195ebf7` after a rebase onto v9. Then: **negative multiples are n.m.** (rulebook B11,
one sweep in the loader, four figures on four companies marked, nothing dropped, check 5 prints the
table every run); **XP Inc's equity-to-AV bridge set to 0** on Daniil's ruling, so it prices at
2.3x on its market capitalisation instead of -3.8x on a negative enterprise value, with the two
supplied bridge components left untouched and the override written into the data file's own header;
**the investor sector column translated** into our archetype vocabulary at read time, which took
callable cards from 738 to 813, the average from 7.2 to 8.0 houses a founder, and the fixtures
getting fewer than three houses from seven to zero, without moving a single core lane; and **the
service shell decided** (750 USD, sole trader, 29 Westbourne Terrace as the address for service).
Full detail with counts in `docs/rulings-applied-4sep.md`. All 14 checks pass, golden rebaselined
deliberately at 0/102.

**Engine hygiene closed.** `tools/check_all.sh` — fourteen checks as of 4 Sep (0 to 13), one
command, all passing —
including the two born from the week's failures: the file-versus-engine intake gate
(`check_intake.py`) and the cross-file duplicate check ("no round votes twice").

**Quiz and maths testing, first versions.** `quiz_walker.py` walks the forks and found seven
questions whose answers reached nothing (now wired or flagged); `funding_model` was required and
unread, now selects the basis; `founder_metric_for()` carries the founder's own figure into every
range — a lender gets five valuations, not five multiples. `check_period_conversion.py` tests the
run-rate conversion against known answers; the identity check (post-money ÷ denominator = stored
multiple, 2% tolerance) passes on all priced rounds.

**Investors, Days 1–2 of 3.** A 445-house table; both layers built and rendered (`investors.js`):
the callable list and the evidence list; tag-overlap ranking on the shared tokeniser; geography
fixed with a country table and a two-founders-must-differ test; compliance rails (no contact
details, no incomplete cards, every card labelled); 92 of 92 callable houses render; 738 cards
and 1,360 evidence chips across the 102 fixtures, 7.2 houses per founder. Daniil's ruling
applied: an unpublished cheque range or geography no longer blocks an active house. The 78-row
enrichment applied as a source, not a patch.

**The revised lending screen loaded** (13 rows in, every row accounted; Atom Bank at 3.17x book —
the second priced book comparable beside Zopa) **and the user-count sweep finished** (224 rounds
read, 79 counts loaded). **The service shell worked through**: sole-trader path viable with
disclaimer text drafted; three retention clocks designed (90 days unsold / 6 years paid /
de-identified kept, with the re-identification caution); Stripe deferred — the pilot invoices.

## (ii) The preceding week — 28 August to 2 September

**The data crisis, and the recovery that ended deeper than before it.** Four screenshot pulls
were lost to compaction; the durability protocol (ten binding rules) was written and has held
since — raw files, manifest, inventory, "no SHA, no work", disputed rows quarantined on sight.
The base was rebuilt and extended: **511 listed names** across five files including the lending
screen (P/E and P/BV — lenders price on book), and **private rounds 90 → 289, all tagged, 179
median-eligible**, through the workbook reconciliation (all five disputed rows ruled — Razorpay's
67.6x gross OUT), the sector screen (40 loaded, AG1 caught as a triple duplicate), and the fix of
the silent tag-join that had hidden a third of the file. The volume-metrics overlay (240 names,
GMV/TPV fenced by scope) and the TPV pull landed; twelve FX conversions ruled and applied.

**The basis-and-period machinery, end to end.** `basis_compatible` (gross never bands with net),
dual-basis rows, period matching, and the standing basis audit — plus Fable's 42-row source
audit (six labels flipped: Zepz, Delhivery, Xpressbees, OLIPOP, Flipkart ×2; Klarna's FX exposed
as a convention fight; all applied). The multi-round rule (Daniil's ordering: scale gates only
when multiples are an order apart, else latest). The lender fence narrowed to the primary
archetype. OFX and EML killed from the universe on Daniil's word. The matcher drops fixed —
bands widen to three names, listed core tops up, and the CONTEXT tier replaced the empty box.
Growth ranking restricted to real CAGRs (`g_rank`). **fork_for's precedence bug fixed** (primary
archetype wins — Mondu prices on book and is asked about its book), the subscription fork
retired, the media fork created, and the tokeniser's "direct to consumer" stopword leak fixed.

**The features and the face.** The investors-and-recommendations strategy written (vcconf and
1752.ai reviewed; two-layer design chosen) and `investors.csv` built. Landing v8.5 and v8.6
shipped: the margin-note round, then the numbered rail, the +40% arrows, the narrowing funnel,
and the investor page. `expected_peers` built as an instrument (255 human-verified peers found;
it drove the matcher fixes).

## (iii) Before that — 24 to 27 August

The foundations. The archetype taxonomy and token weights; the golden suite born and grown to 43
fixtures; growth bands refit on log-normal; the honesty renderer and copy (`honesty.py`,
two-inline rule, severity ranking); the lending book path (Zopa 5.6x); regression gates (R²
floor, extrapolation cap); the at-pricing denominator discipline and the first standing rules;
the pilot launch plan (24 Aug) and the football-field spec. Landing v1 → v8: the headline
("Build a valuation you can defend. / Find the funds to take it to."), the hero card that
expands genie-style into the full field, source blocks, peer hover tables.

---

# WHAT NEEDS TO BE DONE

## (i) Tonight and the next working day — 4 and 5 September

*(Today, 4 September, is the Friday. 5 September is a Saturday: the heading here said "Friday
5 September" and both cannot be true.)*

**Daniil:** (1) **Push tonight's batch. The laptop is BEHIND origin, so a bare `git push` will be
rejected.** Landing v9 went onto `main` through pull request #16 at 18:56 UK today, which is after
Fable wrote the line below saying origin was at `e16c4b4`. Origin is now `0c53510` and the laptop
has never pulled the four v9 commits: `showcase-v9.css` does not exist on the laptop at all. The
sequence is delete the stale lock, commit, rebase onto origin, then push, and the rebase is clean:
the files origin changed (`index.html`, `showcase.js`, `showcase-v9.css`) are byte-identical on the
laptop to their `e16c4b4` versions, so nothing local is competing with them. The uncommitted batch
was verified file by file against origin's own blob hashes at 19:30 UK and is Fable's six items
plus tonight's build: the march (`selector/golden_profiles.py` and 59 new fixtures in
`selector/golden/`, with all 43 pre-existing fixtures byte-identical to origin, so the audit still
holds); Opus's three 3-Sep docs; Fable's two documents; and then
`selector/recommendations.py`, `recommendations.js`, `showcase.css`,
`tools/recommendations_check.py`, `tools/check_all.sh` and this file. No data file is unpushed.
(2) **Three words that unblock builds**: the empty-lane ruling (nine
fixtures pass with an empty lane; Fable recommends three-state scoring — pass both lanes / pass
one with the honest empty state rendered / fail, gate number quotes both); the currency of the
750 (blocks the price on the page and the "Free" meta-description fix); entity name + service
address (disclaimer text is drafted). (3) **Start the SM&CR / outside-business-interest
conversation** — longest lead time, not in your control, gates the first payment. (4) **One
batch pull**: the 17 thin lanes (`docs/prompts/peer-sourcing-4sep.md`) + consumer-software and
edtech funds — the same gap in fixtures and the investor screen, one pull fixes both.

**Opus:** (1) Wire the fix list into `index.html` (the mount div and the script tag): two lines,
deliberately left until the laptop has pulled, because `index.html` is one of the three files
origin changed and editing it before the rebase is how a clean rebase becomes a conflict.
(2) Implement the empty-lane three-state the day the ruling lands. (3) Start the engine-to-reveal
wiring, which is now the only thing between the fix list and a founder seeing it.

**Fable:** verify the pushed march is byte-identical to the 4-Sep audit · read the
recommendations strings for the first ten fixtures · landing round DONE 4 Sep (v9.1, above); left over
from it: the regression hover gains slope and intercept the day A1 lands (an HTML comment marks the
spot), and page 3 still quotes Playfair in £ (`inv-1-body`) while page 2 now says $.

## (ii) Next week — 8 to 12 September

**Wiring week.** Engine → reveal connected: ranges, honesty flags, recommendations and both
investor layers as one payload; the CONTEXT-tier sentence written and the six honesty flags
finally reaching a founder. The reveal render harness (all 102 fixtures headless, screenshot
contact sheet, honesty strings asserted in place — Fable reviews the sheet, not forty tabs).
The quiz walker extended to full branch enumeration as a nightly check; the maths property tests
extended to reveal arithmetic (midpoints, band construction, dilution, bar geometry). The
reviewer workflow — lead → banker review → commentary inside 24h, the actual paid service —
built and exercised once end to end. Legal placeholders off the live page the moment entity and
currency land; price on the page; invoice flow ready. Daniil's sourcing pulls loaded and the
seven under-three-investor fixtures re-scored. **Friday 12 Sep, 18:00 UK checkpoint: dress
rehearsal go/no-go, in writing.**

## (iii) Next month — 15 September to early October

**15–18 Sep: dress rehearsal.** Five to ten friendly founders end to end with real banker
reviews on the clock; fix list only; honesty sentences read in situ; analytics and the cookie
decision; outreach prep; final landing round. **Week of 21 Sep: pilot live.** Daily triage;
nothing new ships but fixes. Fallback pre-committed: if the SM&CR answer has not landed, the
pilot runs free with real reviews and invoices when compliance clears — the date does not move.
**Late September into October (post-launch hardening, in rough order):** revenue-carrying
fixtures (the basis/period machinery on real founder numbers); the 58-round master file wired
once its 14 source URLs land; the 192 listed CAGRs and the growth-band refit that follows; the
listed-side basis tagging (513 rows, the known hole); `asset_intensity` wired into the ecommerce
fork; the 101-row basis backlog burned down inside data touches; deck upload feeding the banker
review (the 1752-style phase 2); Stripe; VAT watch (120 engagements at £750). **1 October:**
this document is archived and `Fairway_STATUS_2026-10.md` starts from scratch.

---

## Open decisions box (as of 4 Sep, 20:02 UK)

- **Page 3 in $ too?** Page 2 is now $; the investor page still says "First cheques £100k to £1.5M".
- **Episode 1's cheque.** The landing said £250k to 3M; `data/investors.csv` (source: episode1.com/faq)
  says 250k to 2m. The 2m from the data file is what page 2 now shows, converted. Say if the 3M had a source.

**Closed 4 Sep, 20:02 UK:** the negative multiple (ruled: n.m., and XP repriced on a zero bridge)
· the investor sector vocabulary (translated at read time, no peer lane moved) · the currency of the
750 (USD) · the entity (none, sole trader) · the address for service (29 Westbourne Terrace, London
W2 3UN, his home address, chosen over the £50-a-year alternative).

**Still open** (Fable's two above, and these; three of them are new):

- **The empty-lane ruling.** Nine fixtures pass with one lane holding nothing. Still yours.
- **The SM&CR answer.** External, longest lead time, gates the first payment.
- **CAB Payments Holdings plc** is the same shape as XP Inc: a payments company whose client
  balances net against its market capitalisation, enterprise value below zero, multiple -2.3x.
  It is n.m. today. It needs the XP ruling applied to it or a different one.
- **The contact email for the disclaimer.** Everything else in that sentence is now filled in.
- **`unifold`** is named in the pull prompt's four hardest gaps and has no row in the gap table.
  Either it belongs in the table or the paragraph should not name it.
- Confirm whether Kriya, Vegrow and Fireblocks Jan-22 were closed by the 3-Sep verdict application.

## Session log — newest first, date and time always

- **4 Sep, 20:10 UK (Opus)** — **D10 broken by me, and the fix is in the suite.** Check 0 runs
  `git status` and `git fetch`, and I ran the whole suite through the bridge, which left exactly the
  `.git/index.lock` D10 exists to prevent (zero bytes, and the bridge will not let me delete it, so
  the next commit block starts with `rm -f` again). `check_all.sh` and `check_pushed.sh` now both
  honour `FAIRWAY_NO_GIT=1`, and an agent running the suite through the bridge must set it. Daniil
  running it in his own terminal should not.

- **4 Sep, 20:02 UK (Opus)** — Batch pushed as `195ebf7` (rebase onto v9 clean, as predicted). Four
  rulings applied and documented in `docs/rulings-applied-4sep.md`: n.m. for non-positive multiples
  (B11), XP Inc's bridge to zero, the investor sector translation, and the three service-shell
  answers. New rule **D14**: a ruling, a rule change or a data pull is committed the moment it
  lands, not at the end of the day, with a TO COMMIT list at the foot of this document for the
  times it cannot be. Part B of the pull prompt reframed: the "empty shelf" reading was half a
  vocabulary fault, and what is left is a narrower and truer gap (three callable houses reach
  Online Learning, fourteen reach Consumer & Prosumer Software and most are generalists).

- **4 Sep, 19:50 UK by the clock (Fable).** Landing v9.1: Daniil's page-2 markup implemented (see
  DONE (i)). Three questions asked and answered before building: regression coefficients wait for A1
  (no invented slope on the live page); WACC held at 18% by the inputs; published £ cheques converted at
  a stated, dated rate. `index.html` and `showcase-v9.css` written to the laptop; they match origin's
  parents exactly, so the commit is a clean `git add -A`. Opus: commit this before wiring the fix list
  into `index.html`, so the two edits stack instead of colliding. An empty branch
  `landing-v9.1-page2-markup-4sep` was created on origin and not used; delete it or ignore it.

- **4 Sep, 19:45 UK (Opus)** — Recommendations Day 3 built and checked (five dimensions live, check
  13 added, renderer written). Push item rewritten: origin had moved to `0c53510` through pull
  request #16 at 18:56 UK, so the "already on origin at e16c4b4" line above was 40 minutes stale
  when it was written and a bare push would have been rejected. Working tree compared to origin's
  own blob hashes rather than through git, per the standing rule about the bridge and the lock file.
  Check 0 added, `tools/check_pushed.sh`: it reports what exists only on the laptop and how far
  local sits from origin, so "committed and ready to push" is checked by the same command as
  everything else instead of being noticed by whoever reads a file list. It runs git in your own
  shell, never through the bridge.
  Part B added to `docs/prompts/peer-sourcing-4sep.md`: the funds for the six founders who are told
  nobody is writing cheques into their sector. Three decisions added to the box. One clock note: the
  "Last updated 20:10 UK" stamp above was written at 19:17 UK, ahead of the clock; timestamps in
  this file should be read off the clock, not estimated. My own first stamp in this entry said
  20:05 and was written at 19:45: corrected at 20:02, same fault, same fix.

- **4 Sep, 20:10 UK (Fable)** — Push item expanded with the verified uncommitted inventory (six
  items, nothing else; lock timestamp confirmed mine and safe to delete). Discoverability note
  added for new chats.

- **4 Sep, 19:35 UK (Fable)** — This document created; tracker and merged roadmap archived. All
  content above verified against origin (e16c4b4) and the audited march state, including the
  stale items the old files disagreed on (fork_for: fixed 30 Aug–1 Sep; tokeniser leak: fixed;
  Klarna/LEAD/pre-money: applied 3 Sep).

## Where the old documents live

`claude/Fairway_tracker.md` (the rolling tracker, 27 Aug – 4 Sep, including the full append-only
log, the durability protocol and standing rules — still binding, preserved there) and
`claude/Fairway_roadmap_MERGED_3Sep.md` (the merged roadmap) are archived in the project with
pointer headers. Repo copy of this document: `docs/STATUS-2026-09.md`.

---

## TO COMMIT (rule D14)

Anything decided or received that is not yet in a commit lives here, with the file it lives in, and
comes off the list when it is pushed. An item here is unfinished work.

*As of 4 Sep, 20:10 UK, one commit behind: the four rulings above, rulebook B11 and D14,
`docs/rulings-applied-4sep.md`, the rebaselined golden suite, and Fable's landing v9.1 (`index.html`,
`showcase-v9.css`) are on the laptop and not yet in a commit. The block is with Daniil.*

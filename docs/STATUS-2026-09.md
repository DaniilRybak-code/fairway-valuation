# FAIRWAY STATUS: September 2026

**The one working document.** It replaces the tracker and the merged roadmap, both of which are
archived (see the foot). Convention, set by Daniil on 4 September: this document runs until
30 September, updated in place, every update stamped with date and time on the UK clock. On
1 October a fresh one starts from scratch (`Fairway_STATUS_2026-10.md`) and this one is stored.
An item moves from TO DO to DONE with a date the same day it finishes; the body never contradicts
itself; "today/tomorrow" never appears without a date.

**Created: 4 September 2026, 19:35 UK (Fable). Last updated: 9 September 2026, 00:30 UK (Fable;
the evaluator's verdicts on the 142 comparable sets checked and Daniil's answers applied: six test
companies re-tagged, a supply-chain software archetype, generic words no longer count towards
closeness, the end-market labels folded, four profiler rules, rules A13 to A16; twenty checks all
green, gate 130 of 142, golden rebaselined 130 of 142 with every move attributed).**

**One term, defined once.** The **No-comps list** is the list of test companies (and, once live,
founders) the engine could not find proper comparables for. It has four kinds of entry: companies
served only on a shared archetype label; companies served on a generic word or the end market alone
(agreed 6 Sep, recorded not removed); companies that got no peers at all; and, from 8 Sep, companies
whose comparable set a person read and struck as irrelevant, which fail the gate. Check 8 prints
all four on every run. Daniil named it on 5 Sep at 23:10 UK; it was briefly called "the register", a
word that means a shareholder register in banking and is not used here again.

**For any new chat in this project:** this is the single source of truth on progress and to-dos.
Find it as `claude/Fairway_STATUS_2026-09.md` in the project docs (or search the project for
"status document"). Read it BEFORE reading any handover or archived tracker/roadmap.

**Pilot launches the week of 21 September 2026.** Rules of the product live in `docs/RULES.md`
in the repo; the data-durability protocol and standing rules from the old tracker are preserved
in the archived copy and remain binding.

**State of the repo at 00:30 UK, 9 September:** origin `main` is at `41d64fa`, Daniil's push of
20:34 UK on 8 September (the field drawn small on the phone's hero, the status document restored).
Every session's CODE is on origin. The working tree holds ONE batch, written over the bridge with no
git run: the evaluator batch of 9 September (re-tags, the supply-chain archetype, the closeness rule,
the end-market folds, the profiler rules, 130 golden files, this document and four new documents).
Listed under TO COMMIT at the foot.

**THIS DOCUMENT WAS OVERWRITTEN ONCE TONIGHT AND IS RESTORED HERE.** `4e080f4` carried a copy of it
written from `91c3d7a`, which dropped the entries of 7 Sep 20:30 and 21:40 (Opus: the forks reach
the page; the consumer-fork ruling) and of 8 Sep 19:20 (Fable: the phone as four screens), and
their rulings. All three are back below, unchanged. Three sessions wrote this file within one hour;
the rule that follows is in the 20:15 entry.

**TWENTY CHECKS (0 to 18 and 20) AND ALL OF THEM PASS**, run in a fresh clone of `41d64fa` holding
the batch. **Gate 130 of 142**, the same count as on 8 Sep with a different composition: fundraisly,
lyka and princeps now pass (re-tagged, re-tagged, the Insurance end market folded); ekho-labs,
lambda-robotics and supercritical now fail on the private lane (one priced round each, the true
state of the pool for freight software, robotics and carbon markets); apollo-atomics, levelten,
mondu and ornadyne fail as before; five struck on reading. **Golden rebaselined 9 Sep, 130 of 142
moved, every move attributed** in `docs/golden-attribution-8sep.txt`: 96 in the closeness label or
the shared-words list only (rule A14), 11 re-tagged fixtures, 10 through the supply-chain rows, 3
through the folded labels, 10 through the eighteen stopwords moving a score by tenths.

**THE THREE HORIZONS (agreed by Daniil, 7 Sep, evening; nothing here changes the launch).** The
strategy review of 7 September (`claude/Fairway_strategy_review_7Sep.md`: the three ideas from
Daniil's friend, angels as the audience, a curated deal-flow gate growing into an index, a
probability of success) settled the frame the product grows in. **Horizon 1, now to December 2026:
the founder utility.** Launch as planned; measure paid conversion at $250 and $750, the share-button
rate, founders who ask for introductions, and the No-comps list. Gate to move on: founders pay, and
at least half of paying founders opt in to sharing. **Horizon 2, 2027: the investor read, sold as
software.** The same reveal read from the investor's side (is this cap defensible, who backed
companies like it, what usually happens to them), sold to angel groups, syndicate leads and SEIS
funds, fed by consented founder profiles. Gate: three paying investor accounts and a two-year
comparison of Fairway-screened companies against the base rates. **Horizon 3, 2028 and after: the
channel, through a partner.** A curated or co-investment product under an authorised manager's
permissions, Fairway paid for the screen. Conditions: the employer position settled, the partner
found, the track record written, the FCA's CP26/28 in its final shape. **Two rules that follow:** the
engine's output stays identical for everyone (that is both the honest product and the unregulated
one), and no session builds towards Horizon 2 before the pilot's numbers are in. **The one
competitor to watch is Equidam** (`claude/Fairway_competitor_Equidam_7Sep.md`): checked quarterly,
first check early December 2026; the two moves that would matter are engine-selected comparables and
a sourced private-round set shown to founders.

---

# WHAT HAS BEEN DONE

## (i) Yesterday and today: 4 and 5 September

**The evaluator's verdicts, checked and applied (Fable, 8 and 9 September).** An independent
evaluator marked the 142 comparable sets: 100 OK, 20 OK (weak), 13 FLAG, 4 CONFIRM OUT OF MARKET,
5 ALREADY STRUCK. Every flag was walked through the engine's fences and every "better name in the
pool" looked up (`docs/evaluator-verdicts-review-8sep.md`, project copy
`claude/Fairway_evaluator_verdicts_review_8Sep.md`). Of the 13 flags, 6 fair as written, 3 fair
on the verdict but wrong about the cause, 2 partly fair, 2 pushed back (emergent, derya). **What
caused them, counted:** six were the TEST COMPANY's own tags describing the customer instead of the
company; four were one archetype (Commerce Enablement & Fulfilment: 26 operators and REITs, 2
software rows) filed in the consumer family, walling supply-chain software off from Kinaxis and
Manhattan; one the label-only fallback pricing without saying so; one a pool gap. Daniil's answers
of 8 Sep, applied the same night: **six fixtures re-tagged** (fundraisly on the investor-platform
angle: ZoomInfo, Similarweb, HubSpot in the wider ring, Clay, Apollo.io, 6sense, gate FAIL to PASS;
alloovium: Procore, Trimble, Bentley; kita software-first: Fair Isaac, Blend, Q2, Jack Henry, FIS,
Mambu; lambda-robotics: Vention and Applied Intuition alone, gate PASS to FAIL; supercritical as a
levelten twin: IEX, CME, MCX, Xpansiv unpriced, gate PASS to FAIL; lyka to the pool's pet label:
BARK 30.7, gate FAIL to PASS). **Route A built** (rule A15): `Supply Chain & Logistics Software` in
the software family, nine rows across three files, ekho-labs (Kinaxis, Tecsys, Manhattan, Samsara
pinned on his word; E2open alone privately, gate PASS to FAIL), bizmark (Kinaxis and Tecsys in),
derya (Kinaxis, Manhattan, SPS Commerce; his Salesforce reading, the company's own page recorded
beside it). **Generic words no longer count towards closeness** (rule A14): the label and the
shared-words sentence ignore words carried by 25 or more companies, eighteen stopwords, weight file
405 in and 400 out with the five dropped named; gate rule 4 narrowed to catch-all archetypes with no
DIRECT lane. **The end-market labels folded** (TAKEOVER 7 done): 10 private rows, 204 in and 204 out;
florin gets Guidewire, FINEOS, Crawford, ZhongAn, Trupanion; princeps passes on Crawford, Trupanion,
CCC. **Four classification rules in the profiler prompt**, asserted by check 17 (rule A13). **E1
measured at zero fixtures moved** once lyka was re-tagged; **E2 measured two ways and rejected**
(it lets Rent the Runway, Verint and Microsoft through elsewhere). **Rule A16** (the comp-of-a-comp
lookup) written for the bulk pass. **Sourcing list** `docs/sourcing-list-8sep.md`. Full record of
each answer and what it became: the APPLIED section of the review document.

**Daniil's five rulings of 6 September, applied the same morning (Opus, 09:45 UK).**

1. **A founder who gives no numbers at all still gets a reveal.** His words: "what if the user does
   not want to give us the numbers at all? Then we should be able just to show the peers and how
   they trade", as up to four bar charts: public multiples, private net, private gross, private per
   user. Mostly already true, because a range counts the comparable rounds that exist and only the
   PRICE needs a founder figure. The gap was the per-user chart, offered only to five consumer
   archetypes or to a founder who had already typed a count, so it reached 6 of 102 fixtures. It is
   now also offered where the founder's own picked rounds can price it, two rounds minimum, which is
   the same evidence test everything else uses. **Per-user chart 6 to 54 of 102. Fixtures able to
   show three or four charts, 33 to 63.** The payload carries a `charts` list in his reading order,
   each entry saying how many names stand behind it and whether a founder figure exists to price it.
2. **The free tier: public fully free, everything non-public blurred, and hovering a blurred range
   shows the comparable names without their multiples.** Built as `build(prof, tier='free')`.
   **This amends rule E8**, which said a locked lane is absent from the payload rather than blurred,
   and the amendment is recorded there: the NAMES are now shown, the FIGURES still never reach the
   browser, so the blur is drawn over nothing and there is nothing to inspect. Check 14 walks all
   102 free payloads and asserts that no multiple, low, mid, high or founder value appears anywhere
   outside the public lane, in the lanes, the charts or the lead. **A gap this found:** the private
   lane has never carried a hover table at all, only the listed one did, so there was no list of
   names to show. It has one now, with the round date beside each name, which the listed table does
   not need and a round does.
3. **The growth definition, and the load it unblocks.** "CY+1 to CY+3 growth is a CAGR of CY+1 to
   CY+3. It is 2026, so in our case it is 2026 to 2028." So CY+1 is the current year on his screens,
   and CY+1 to CY+3 is the same window as the other files' CY+0 to CY+2. **Tested rather than
   assumed:** the pull carries the year-on-year series beside the CAGR, and compounding the CY+2 and
   CY+3 rates reproduces the printed CAGR on 16 of 16 rows, against 2 of 16 for either alternative.
   The code comment calling the two "anchored a year apart" and "NOT the same measure" was wrong and
   is corrected with the measurement in it. **The public pull is no longer blocked on a ruling; it
   is blocked only on the work of loading it** (see TO DO).
4. **`investor_check.py` brought into line with the 3-Sep cheque ruling.** It refused 18 callable
   houses; the engine has rendered them since the day of the ruling, so the same suite run was
   contradicting itself (`investors.py` 156, this check 138). A missing cheque is now a soft note
   and the card says "First cheque not published". **Refusals 18 to 5**, all five on a missing stage
   band, which is the half of the ruling that still holds the line. *One gap left, named not fixed:*
   the engine does not require a stage band at all, so it still renders 156 against the check's 151.
5. **Udemy released into the medians.** His instruction: an all-share transaction is valued on the
   acquirer's share price at the transaction. Reading the 8-K exhibit already cited in the row shows
   the figure we hold IS that method written the other way round: the release states the combined
   implied equity value of about $2.5bn is "based on the closing prices of Coursera and Udemy common
   stock on December 16, 2025", and Udemy holders take 41 per cent fully diluted, so $1,025m is the
   market value of the stock consideration at the transaction date. Applied by
   `tools/apply_udemy_ruling_6sep.py`, idempotent: 268 rows in, 268 out, one changed, none dropped.
   It prices on the GROSS lane only, so it reaches a founder who gives a gross figure and no other.
   It now sits in five fixtures' picked sets (honen, inato, bloomy, wondering, befreed).

**`basis_compatible()` deleted (Opus, 6 Sep, on Daniil's word).** Never called by anything, described
as the gross-versus-net fence in four documents, and the real fence is `basis_mult()`, which has done
the job since it was written. Everything the dead function knew is now in `basis_mult`, whose
docstring says it is the only fence and that changing it is changing rule B3: the lender guard,
without which all four lender fixtures silently emptied in August, and the rule that an absent basis
reads as net-equivalent rather than as a mismatch. The two 31-August documents that describe it keep
their text and carry a dated correction; the live instruction in
`docs/brief-for-fable-basis-and-period.md`, which told a future session to wire the deleted function
onto the listed lane, is corrected outright.

**Net and gross revenue both, built and checked (Opus, 5 Sep, 23:55 UK, in the TO COMMIT batch).**
Daniil's ruling of 23:35 UK, in the engine. *What was wrong, in numbers:* of 320 private rounds, 78
are priced on a gross figure and 66 of those hold a usable multiple and sit in the medians, and every
one of them was unreachable because the quiz asked every founder for net revenue only. Across the 102
fixtures, 41 have a gross round sitting inside the peer set the matcher had already picked for them,
105 such rounds in all. *What the quiz asks now:* every fork that asks for revenue asks for both
figures, optional in all of them, with the difference explained in plain words beside the question.
That is eight forks. Lending is the one that does not ask, because a lender is not asked for revenue
at all, and its 13 gross rounds are now a named gap rather than an accident. *What the engine does:*
a second reading, `REVENUE_GROSS`, alongside the net one. The founder's gross figure meets only rounds
priced on gross, the net figure only rounds priced on net, and they produce two separate labelled
ranges. **Rule B3 is not loosened; splitting the two is what makes the gross rounds usable at all.**
Six new assertions in check 9 hold that in place: a net row is silent in the gross range and a gross
row silent in the net one, an unlabelled row reads as net-equivalent and is never swept into both, the
gross figure takes the same period conversion, and each range multiplies by its own founder figure.
**Private lane only, deliberately:** the listed files carry no basis column at all (513 rows), so a
listed gross range would mean relabelling rows nobody has read. Written up as rulebook **B3a**.
**Gate 90 to 95 of 102:** clera, nursa, paymentkit, standout and tsenta flip to PASS, nothing
regresses. *One decision inside this that is really Daniil's, and it is in the box below:* the gross
reading is offered whether or not the founder answered, which is how the net reading has always
behaved. Measured both ways: ungated the gate is 95, gated on an answered question it is 90.

**The No-comps list named in the code, and A12 finished (Opus, 5 Sep).** The word "register" is gone
from the code and the rulebook. Check 8 prints a section headed **THE NO-COMPS LIST** with both kinds
of entry under it: kind 1, served on a label, 14 comparables across 2 lanes; kind 2, not served at
all, 7 of 102 fixtures each with its reason. Two counts and not one sum, because a fixture can be in
both at once (ultrasonium is rescued on its private lane and fails anyway) and an inflated brief is a
worse brief. **A12 part 3** rewritten to Daniil's 23:10 wording: four marches, the list resolved in
one bulk pass after the 18 September march, no lane-by-lane chasing in between, and the rule says in
as many words that a proposal to pull data outside that pass is the rule telling you to stop. **A12
part 2** restated as what the code does: the fallback opens for a lane holding fewer than two priced
comparables and is kept only if it adds a priced name. **A11** had a heading and no body; it now says
hardware is filed under what it sells rather than under a hardware archetype, and records the
uncomfortable consequence that ultrasonium carries Owned-Inventory Retail and was offered Moove and
Quince on that label.

**The thin-lane diagnosis no longer contradicts itself (Opus, 5 Sep).** It printed "12 thin lanes, 0
genuine sourcing requests" on the evening a pull into those lanes moved eight fixtures. The cause was
that any held name stood a sourcing request down, including a name the relevance gate rightly bars
(Docker for paymentkit, Zepz for tash). A name is now a candidate only if sourcing is not what stands
between it and the founder, and the headline is three numbers: **3 sourcing requests** (goldfish and
honen on secondary, levelten on private), **8 awaiting a ruling**, **1 matcher question**.

**The public-pull dates corrected in the repo copies (Opus, 5 Sep).** The raw file and
`docs/public-pull-verdicts-4sep.md` both said 4 September. Both first exist in commit `224ec60`, made
at 21:28 UK on 5 September, and the header also claimed a 21:45 transcription time that is later than
the commit carrying it. Both now say 5 September and claim no exact minute. **The file names still say
`2026-09-04` and `4sep` and are deliberately unchanged:** they are referenced by the MANIFEST, check 1,
the verdicts document and this document, so the correction is written inside the record instead.

**The fix list wired into the page (Opus, 5 Sep).** `recommendations.js` has its mount
(`recommendation-blocks`) and its script tag in `index.html`, above the investor block, so a founder
reads what moves their range before who to take it to. The CSS was already there. Like `investors.js`,
nothing calls the renderer yet: that is the wiring below.

**Engine to reveal, started (Opus, 5 Sep).** `selector/reveal_payload.py` assembles the whole reveal
in one pass over the engine, and `tools/check_reveal_payload.py` is **check 14**. Today the football
field is drawn from figures typed into the HTML, the two renderers each expect their own payload, and
the honesty strings reach nobody at all; four sources means four chances to show a range from one run
and a caveat from another. One object now carries every lane on every basis with the founder's own
figure beside each, the honesty caveats split into inline and behind-the-disclosure, the fix list,
both investor layers, and the named comparables behind each lane, all from ONE selection run. Across
the 102 fixtures: 102 build without an exception, 96 carry at least one caveat (which reach a founder
for the first time), 101 carry a fix-list dimension, 102 carry a callable house, and 32 lanes offer
both revenue readings side by side with no company in both. Two things deliberately absent: **no
price** (multiplying is the page's job, so the arithmetic shown is the arithmetic the hover explains)
and **no locking** (rule E8 makes the free and paid tiers two different objects, and the lock ruling is
still open, so `lanes()` is where that filter goes and nothing guesses at it).

**Golden rebaselined, 93 of 102 fixtures, and the reason (Opus, 5 Sep).** Nothing that was recorded
before moved in value: checked key by key, zero fixtures changed anything outside `all_ranges`. Inside
it, two things happened. It **gained** the private `REVENUE_GROSS` entry wherever a gross round prices.
It **lost** a `listed` sub-dict that was pure duplication: for a fork with one listed basis,
`all_ranges['listed']['REVENUE']` restated `core_range` line for line and reached nothing, because the
gate reads `all_ranges` under core, secondary and private and never under listed. Each lane is now
recorded only when that lane holds more than one reading. The nine lending and exchange fixtures that
already carried both lanes are byte-identical. 102 fixtures in, 102 out, no data row touched.

**Rule A12 in the rulebook: failing the relevance gate is a failure, the archetype fallback is how
we serve the founder anyway, and every use is recorded (Opus, 5 Sep, pushed 22:34 UK as `d1f8966`).**
Plain English: a comparable normally has to share product vocabulary or a specific end market with
the founder; sharing only an archetype label (the coarse business-type bucket every company carries)
is not enough. The fallback is a rescue: it opens only when a lane holds fewer than two priced
comparables and it is kept only if it adds a priced name. It fired for 2 of 102 fixtures and moved
the gate 89 to 90. Every name it admits is printed on every run of `tools/peer_universe_check.py`.
*What the two firings served:* blindspot (billboard booking) passes on HubSpot, Klaviyo, Freshworks,
Braze, Sprout Social and Text S.A. on the label Marketing & Customer Engagement; ultrasonium (metal
manufacturing, OUT_OF_MARKET) was handed Moove, Quince, Olive & June, Flink, Octopus Energy and
Berkshire Grey on the label Owned-Inventory Retail, and it fails anyway.

**Daniil's clarification of the rule (5 Sep, 23:10 UK), which replaces the third part of A12 as Opus
first wrote it, and is now IN the rulebook.** The point of the rule is to stop spending two to three
hours a day enlarging the data pool. The pool is large enough; there will always be blind spots; the
pilot is the best way to find them. The time goes to the landing page (still a lot to do) and to
testing the quiz (not started). So: **on 8, 11, 15 and 18 September we do a march, not a data pull**:
30 to 40 new test companies each time, taken the way the 3-Sep march took them. Each march adds to
the No-comps list (both kinds of entry). **The No-comps list is resolved in bulk once, ahead of the
launch**, not lane by lane as it grows. **In the meantime the work is on the matching machine itself,
including the tagging**: that is a separate to-do for Fable, with its prompt, in TO DO (i) below.

*Why a company can get no peers at all from a database this size, in plain words.* A test company
fails the gate when one of its two lanes (listed companies, private rounds) has fewer than two
comparables that carry a usable price. Names alone are not enough. For the eight that failed on the
private lane last night, the database did hold names in the right archetype, but the round either
disclosed no revenue, or disclosed it on a gross basis while the test company defaulted to net.
**Six of those eight are fixed by the net-and-gross change above.** The two that are not, levelten
and manifold-robotics, are different problems: levelten's second round carries no revenue multiple
at all, and manifold-robotics has only one round in its picked set.

**The taxonomy measured (Opus, 5 Sep, `docs/taxonomy-review-5sep.md`).** 34 archetypes over 1,281
slots; the big four (Vertical Software, Lending & Credit, Data, AI & Developer Tools, Business
Applications) carry 32 per cent; Vertical Software holds 120 rows spanning stock exchanges, core
banking, auto classifieds and restaurant software. Proposal, not done and not before the pilot: for a
defined list of catch-all archetypes, an archetype match only counts when the industry field matches
too. About twenty lines, no re-tag. Ruling open (see the box).

**Claude's 5-Sep pull, delivered, committed and loaded (Claude 21:15 UK; raw commit `224ec60` at
21:28 UK; load commit `dad1b3f` at 21:54 UK, D14 order kept).** 27 private rounds for 24 companies
across the eighteen thin private lanes plus three of the four control transactions the 4-Sep work order
names (Sapiens $2.5bn, Accolade $621m, Udemy 41 per cent of a $2.5bn combined value), every figure read
on its URL and quoted in the row; about 460 candidate examinations, every rejection named with a reason
code in `docs/handover-2026-09-05-claude.md`. 24 tag rows. 19 investor houses (17 CALLABLE, 2
EVIDENCE) for Consumer & Prosumer Software and Online Learning. **Counts reconciled:** 27 rows
in, 27 in the engine (check 2 passes); 19 houses in the raw file, all 19 present in `data/investors.csv`
(Y Combinator was already there, so the table gained 18 new houses, which is why the load commit says
18 and the handover says 19). Investor table 445 to 506 houses, callable 92 to 142. **Gate 81 to 89**:
agentcard, bizmark, finn, marble, osseus, payna, priori-legal and wispr-flow flip to PASS, nothing
regresses. The basis trap the order warned about held: Mercor, Micro1, Juspay and Cashfree land in the
clera, standout, nursa, tsenta and paymentkit lanes and priced none of them, because every disclosed
figure there is gross and the fixtures defaulted to net. **That is exactly what the 23:35 ruling
fixed.** Four rows held out of the medians for a ruling (Moss, Restaurant365, AuditBoard: floor over
floor; Udemy: no standalone price). Not done: LTG (offer value not on a reachable page; the LTM
denominator, GBP 528.0m, is in the handover), and Confluent and Verint, which the revised order added
after that session started. Search was capped at 200 calls, so the "no source" rejections are weaker
than the kept rows; the re-check list is in the handover.

**Second load of the evening, and the floor ruling applied (Daniil's commits `106b0df` and `6433348`,
23:21 and 23:22 UK).** Investor round two (24 houses, table 506 to 532), three control transactions,
and the floor-over-floor rows released: **Moss (14.29x), Restaurant365 (10.0x) and AuditBoard (15.0x)
are back in the medians.** Udemy stays held at 1.29x with `in_medians` false, which is the separate
question of a company with no standalone price. Open decision 1 in the box is therefore closed except
for Udemy.

**The public pull of 16 names transcribed and read (Opus, 5 Sep, 21:28 UK).** No CSV exists or will:
the pull runs in a separate sandbox, so `data/raw/2026-09-04_public-pull-16-names.csv` is the record
(D1) with a MANIFEST row. Arithmetic verified on all 16. Seven issues in
`docs/public-pull-verdicts-4sep.md`. Deliberately NOT loaded, which is why check 1 is red. Of the
three questions that blocked the load, two closed the same evening: **the currency of the two Euronext
rows is USD** (rate derived at USD/EUR 1.0748 from Pluxee's own net cash of EUR 1,270m against its
screen bridge of -1,365), and **the customer-float adjustment is ruled OUT by Daniil, his figures
stand** (P/E is on market capitalisation and untouched by the bridge; Edenred 12.5x against Pluxee
10.0x is two companies priced sensibly; the enterprise-value gap is a real balance-sheet difference).
Both Euronext rows re-pulled 5 Sep and moved slightly. A flag survives for whoever bands the pair: on
revenue they read 2.8x against 0.8x, so the earnings multiple is the comparable measure for these two.
One question still blocks the load: the third growth definition (CY+1 to CY+3).

**The thin-lane diagnosis rewritten (Opus, 5 Sep, in `224ec60`)** so that it no longer reports a lane
as a sourcing request while a name we already hold sits behind the relevance gate (it reported
orchids as a request while listing Wix). `tools/check_proposed_names.py` now refuses a ticker list
containing names already in the file, after eighteen of 41 proposed tickers turned out to be held.
Rulebook **C7**: a listed comparable has to still be listed; Sapiens, LTG, Udemy and Accolade no
longer trade and were re-targeted as control transactions under B6. Both pull documents rewritten on
the new division of labour: Claude does private rounds and investor houses alone, Claude selects
public NAMES, Daniil pulls Capital IQ. *The caution recorded in the 22:55 audit is now resolved:* the
"0 genuine sourcing requests" headline was wrong and the tool has been fixed tonight (see above).

**Landing v9.1 complete and pushed (Fable; round 1 on 4 Sep 19:50 UK inside `74f0c7d`, round 2 on
5 Sep 20:05 UK as `236fd6b`).** Daniil's page-2 markup: the Northsteer card in $ (illustrative figures
swapped 1:1; the three UK funds' published £ cheques converted at 1.35 $/£, Trading Economics 4 Sep,
and the table says so); the four $0.78m cells and the $0.64m cell carry a pop-up with the arithmetic
($50k a month, 60 per cent plan, 4.0 per cent a month, twelve months added, the same sum
`app-growth.js` does); every bar's hover reads "$x of your metric times the range: $a to $b" and all
seven reconcile; the regression hover explains R² in plain words; the DCF hover shows the build-up
landing on 18.0 per cent (5.27 per cent 30-year Treasury, FRED 2 Sep; beta 2.0; 6 per cent MRP; cost of
debt 22 per cent; debt 15 per cent; no tax shield), Daniil's ruling being to hold 18 per cent until A1.
Round 2: the three Our read columns end on one line; the universe box shows Appian, HiBob and
Factorial; likely investors gains Seedcamp ($350k to 1.25M) and Connect Ventures ($500k to 5M) from
`data/investors.csv` with source URLs; note 6 leads with investors and fixes. Left over: the
regression hover gains slope and intercept the day A1 lands (an HTML comment marks the spot), and
page 3 still quotes Playfair in £ (`inv-1-body`) while page 2 says $.

**The 100-fixture gate: PASSED (3 Sep) and re-scored since.** 43 to 102 real fixtures in one
evening under Daniil's re-framed gate (peer-universe depth and breadth, no revenue figures, companies
taken randomly from YC and Product Hunt); audited by Fable on 4 Sep (all checks pass, golden 0/102,
the five least-trusted tags verified against the companies' own pages, a planted comparable surfaced
at rank 1). Re-scored 4 Sep on Daniil's ruling that a fixture passes only with at least one public
lane and one private lane (rule 0 in `peer_universe_check.py`): 90 became 81. Then 89 after the 5-Sep
pull loaded, 90 with the rescue, and **95 of 102 tonight** with net and gross both. The 7 still
failing: two on the private lane (levelten, manifold-robotics, 1 priced round each); two with no
public lane (apollo-atomics, lyka); fundraisly (every lane thin overlap); ultrasonium (both); and
tash, which has one net round and one gross round, so each lane holds one priced name and one name is
not a range. Three pass with an empty secondary lane (acti, clera, osseus), allowed since 4 Sep.

**Recommendations Day 3 closed (Opus, 4 Sep).** `unit_economics` reads gross margin against the peer
set and says when the founder's field is already drawn on gross profit; `market_position` prices the
founder's own lane against their whole family and names what the DIRECT / ADJACENT / BROAD label costs.
Renderer `recommendations.js` on the investors.js contract (whitelist, three sentences a card, nothing
rendered when there is nothing to say). **Check 13** recomputes every figure a dimension declares from
the values it claims to come from. Market position renders for 99 fixtures, evidence gaps for
101, and the three revenue-dependent dimensions for none, correctly, since no fixture carries revenue.

**Four rulings applied and pushed 4 Sep (Opus, `74f0c7d` and `e35e96e`).** Negative multiples are
n.m. (rulebook **B11**; four figures on four companies marked, nothing dropped, check 5 prints the
table every run). XP Inc's and CAB Payments' equity-to-AV bridges set to 0 on Daniil's ruling
(`tools/apply_bridge_zero.py`, idempotent): XP 2.3x revenue, CAB 1.6x revenue with its gross-profit
multiple n.m. because its gross profit is negative. The investor sector column translated into our
archetype vocabulary at read time. The service shell decided: 750 USD, sole trader,
29 Westbourne Terrace as the address for service. **Pricing is three tiers**: free reveal with lanes
locked, $250 for the full field, $750 for a valuation advocacy presentation taking up to a week
(`docs/service-shell-decisions-3sep.md`; rulebook **E8**, a locked lane is absent from the payload,
never blurred). New rule **D14**: a ruling, a rule change or a data pull is committed the moment it
lands. **D10 was broken once** (the suite run through the bridge left an index.lock); `check_all.sh`
and `check_pushed.sh` now honour `FAIRWAY_NO_GIT=1`, which an agent must set and Daniil must not.

**The eight-figure data audit closed and applied (Fable and Opus, 3 and 4 Sep, commit e9ab546):**
Klarna restated at pricing-date ECB rates with the entity named ($1,208.8m / 37.7x Jun-21,
$1,298.8m / 5.16x Jul-22, Klarna Bank AB); LEAD School on printed revenue-from-operations at 142.17x;
Starling and Upgrade flagged Pre-money; Atom Bank tagged pre/post-unspecified; Oura carries its
source-upgrade note; Dream Sports, Discord, Konfio and Tala hold.

**Engine hygiene.** `tools/check_all.sh`: **fifteen checks (0 to 14) as of 5 Sep**, one command,
including the file-versus-engine intake gate (`check_intake.py`), the cross-file duplicate check ("no
round votes twice") and, new tonight, check 14 on the assembled reveal payload. `quiz_walker.py` walks
the forks; `check_period_conversion.py` tests the run-rate conversion and, since tonight, the gross
figure and the six basis-side cases; the identity check (post-money divided by denominator equals the
stored multiple, 2 per cent tolerance) passes on all priced rounds. **Investors, Days 1 to 3:** a
532-house table; both layers rendered (`investors.js`); geography fixed with a country table;
compliance rails (no contact details, no incomplete cards, every card labelled); 156 of 156 callable
houses render; 813 cards across the 102 fixtures. The revised lending screen loaded (13 rows,
Atom Bank at 3.17x book beside Zopa) and the user-count sweep finished (224 rounds read, 79 counts
loaded). The service shell worked through: sole-trader path with disclaimer text drafted; three
retention clocks (90 days unsold / 6 years paid / de-identified kept); Stripe deferred, the pilot
invoices.

## (ii) The preceding week: 28 August to 2 September

**The data crisis, and the recovery that ended deeper than before it.** Four screenshot pulls
were lost to compaction; the durability protocol (ten binding rules) was written and has held
since: raw files, manifest, inventory, "no SHA, no work", disputed rows quarantined on sight.
The base was rebuilt and extended: **511 listed names** across five files including the lending
screen (P/E and P/BV, lenders price on book), and **private rounds 90 to 289, all tagged, 179
median-eligible**, through the workbook reconciliation (all five disputed rows ruled; Razorpay's
67.6x gross OUT), the sector screen (40 loaded, AG1 caught as a triple duplicate), and the fix of
the silent tag-join that had hidden a third of the file. The volume-metrics overlay (240 names,
GMV/TPV fenced by scope) and the TPV pull landed; twelve FX conversions ruled and applied.

**The basis-and-period machinery, end to end.** The gross-never-bands-with-net fence (written then
as `basis_compatible`, which turned out never to be called and was deleted on 6 Sep; `basis_mult` is
and always was the fence that runs), dual-basis rows, period matching, and the standing basis audit, plus Fable's 42-row source
audit (six labels flipped: Zepz, Delhivery, Xpressbees, OLIPOP, Flipkart twice; Klarna's FX exposed
as a convention fight; all applied). The multi-round rule (Daniil's ordering: scale gates only
when multiples are an order apart, else latest). The lender fence narrowed to the primary
archetype. OFX and EML killed from the universe on Daniil's word. The matcher drops fixed:
bands widen to three names, listed core tops up, and the CONTEXT tier replaced the empty box.
Growth ranking restricted to real CAGRs (`g_rank`). **fork_for's precedence bug fixed** (primary
archetype wins; Mondu prices on book and is asked about its book), the subscription fork
retired, the media fork created, and the tokeniser's "direct to consumer" stopword leak fixed.

**The features and the face.** The investors-and-recommendations strategy written (vcconf and
1752.ai reviewed; two-layer design chosen) and `investors.csv` built. Landing v8.5 and v8.6
shipped: the margin-note round, then the numbered rail, the +40 per cent arrows, the narrowing
funnel, and the investor page. `expected_peers` built as an instrument (255 human-verified peers
found; it drove the matcher fixes).

## (iii) Before that: 24 to 27 August

The foundations. The archetype taxonomy and token weights; the golden suite born and grown to 43
fixtures; growth bands refit on log-normal; the honesty renderer and copy (`honesty.py`,
two-inline rule, severity ranking); the lending book path (Zopa 5.6x); regression gates (R²
floor, extrapolation cap); the at-pricing denominator discipline and the first standing rules;
the pilot launch plan (24 Aug) and the football-field spec. Landing v1 to v8: the headline
("Build a valuation you can defend. / Find the funds to take it to."), the hero card that
expands genie-style into the full field, source blocks, peer hover tables.

---

# WHAT NEEDS TO BE DONE

## (i) Sunday 6 September and Monday 7 September

**Daniil, rulings still open (each one is a rule, so yours). Written the plain way this time.**

1. ~~**Should a per-user chart that is too wide be hidden?**~~ **CLOSED 7 Sep without a number**: the
   enterprise-contract readings went at source (`PER_CUSTOMER_BUSINESS_MAX`, his catch), the widest
   chart fell from 1,034x to 97x, and there is nothing left to hide (log, 17:40 UK).
2. ~~**When the founder's growth is unknown, what stands in for next-twelve-months revenue?**~~
   **CLOSED 7 Sep**: the trailing figure stands in, on both readings, with a warning saying which
   way the error runs (log, 17:40 UK).
3. ~~**May the amount the founder is raising leave the browser?**~~ **CLOSED 7 Sep, and the answer
   was that the question was wrong.** Daniil: "can we not do the work of refining the universe of
   potential investors without me actually seeing what amount they are raising? How is it possible
   we are able to build a football field for them without me seeing the revenue figure... In any
   case, I do not think we need to compromise here on privacy." Right on both counts. It is the
   football field's own trick: the server sends the houses WITH their published cheque ranges, which
   are our data about funds and not the founder's data about themselves, and the browser drops the
   ones that cannot fund the round. **Measured on all 102 fixtures at a $500k raise, the browser
   path gives the identical list to sending us the raise, 102 of 102.** The raise is not added to
   the allowlist. The rule now lives in two languages, so check 15 runs both copies over every
   distinct cheque shape in the investor table at five raises and fails on the first disagreement.
4. ~~**If the computer running the checks has no "node" installed, should check 15 stop the suite?**~~
   **CLOSED 7 Sep: fail**, which is what the code already did (Opus's status patch, `97c90b5`).
5. ~~**How much debt makes a listed company's multiple stop describing a business?**~~ **CLOSED 7 Sep
   on the half that mattered**: a flagged name is never kept out of a band; it "should not be a
   barrier for showing that in the comps set if the comp is relevant from business point of view"
   (his words, log 17:40 UK). `LEVERAGE_FLAG_PCT` stays at 80, the recommendation, unchallenged.
6. ~~**Should the 102 fixtures carry a stage?**~~ **CLOSED 7 Sep**: every fixture carries Seed as a
   stated test condition (his ruling, log 17:40 UK), so the live investor path is exercised by the
   suite for the first time since 6 September.

**New 7 Sep, from the strategy review, both optional: nothing changes if they stay unruled.**

7. **Should the pilot ask one question after the reveal: "Would you want this report sent to
   investors that match your company?"** One survey line, no build; it measures the supply side of
   the friend's channel idea from inside the pilot. Recommendation: yes.
8. **Should the pilot count founders who ask for an introduction?** A tally, not a feature, and no
   introduction is made (making one is arranging under the FCA rules; see the review).
   Recommendation: yes.

**New 9 Sep, from the evaluator's verdicts, needing Daniil (none blocks the launch):**

9. **Hop-aero and ultrasonium: strike or keep?** Hop-aero passes the gate today on FedEx, UPS and
   Gopuff, the class you struck on 8 Sep; ultrasonium passes on PTC, Dassault, Lectra by your own
   6 Sep ruling. Both untouched until you say.
10. **Publora: pin Buffer?** Buffer is the best product match in the private pool (1.6 points) and
    is cut only because its one round is October 2014. Your call.
11. **Should generic words also score zero in the selection itself?** Rule A14 stops them counting
    towards the closeness label and the shared-words sentence; they still clear the relevance gate,
    because zeroing them was measured on 6 Sep to cost real passes (acti, rainforest, tienda-pago,
    mondu). Widen it or leave it.
12. **Derya: your reading (vertical software, Salesforce-like) or its page (a services-led broker
    that has handled $15m of freight)?** Tagged your way; the page's reading sits on the profile.
13. **Three listed training-data vendors** (Appen, Innodata, TELUS Digital) are on the sourcing
    list against your 8 Sep reading that none exists; confirm before they are pulled.
14. **The comp-of-a-comp rule (A16): in the bulk pass after 18 Sep, or now?** Written for the bulk
    pass under A12 part 3.

**New 9 Sep, tasks (Fable or the next session):**

- **Analyse Fundraisly and replicate what fits.** Daniil: it is our investor-recommendation tool
  "on steroids". Its own comparison table claims a 300k+ investor database, investor intelligence,
  warm outreach, targeted outreach, service (data-room advice, review, process assistance), warm
  paths from unicorn founders, at $1k to 5k a month, against agencies, fundraisers, Raisi, Metal,
  OpenVC, Boardy, Dealroom and PitchBook. Ours: 533 houses, callable cards on sector and stage,
  cheque filter in the browser, no outreach. The task: read their product end to end, list what
  they do that we do not, say which parts are software we can build (intelligence, matching depth,
  the "who backed companies like yours" layer) and which are a service or a network we cannot
  (warm paths, outreach), and mind the FCA line: making an introduction is arranging. Write-up
  first, no build.
- **The label-only fallback lane** (cause 3 of the review, parked on Daniil's word): a lane served
  on the archetype fallback still prices at the ADJACENT band and the founder is told "priced off
  businesses in your category", never "these share only a label with you". Eleven lanes today
  (check 8, kind 1). Revisit after the bulk pass; Daniil expects re-tagging and data to shrink it.
- **The Design & Engineering split**, post-pilot with the taxonomy rule: sixteen listed rows mix
  CAD, EDA and PLM vendors with design tools and robotics platforms (orchids, 21st, chronicle,
  emergent). Same shape as Route A, smaller.
- **A Pet Care end market** so lyka, BARK, Chewy and Trupanion share a label instead of borrowing
  Retail & E-commerce; taxonomy build.

**Daniil, actions:** run the commit at the foot. Start the SM&CR / outside-business-interest
conversation if not started: longest lead time, gates the first payment, and since the strategy
review it is also the conversation that decides whether Horizon 3 is ever possible. Rulings 7 and 8
above if you want them in the pilot.

**Opus, DONE 7 September. All three, plus four things found on the way.**

1. **The public pull of 16 names is LOADED.** `tools/load_public_pull_16_7sep.py` reads the
   5-September transcription and writes `data/peers-mixed.csv`, a sixth peers file loaded as
   SECONDARY so that no existing archetype is re-counted. **16 rows in, 14 out, 2 named:** LivePerson
   excluded on the delisting notice, Green Dot held out because it has no gross profit and no BVPS
   or P/BV and belongs in `peers-lending.csv`. Not one figure is retyped: the script is a pure column
   rename from the raw file, which is the only defence against a second transcription error on a
   file that was itself transcribed from a screenshot. Tags are hand-written in
   `data/peers-mixed-tags.csv`. Check 1 is green for the first time since 5 September.

   **Golden moved on 100 of 102 and every move is attributed:** 21 fixtures gained one or more of the
   fourteen new names, 15 moved because the new tags file made five words generic (`loop`, `plans`,
   `process`, `team`, `workflow`) and the token-weight file was regenerated, and 64 moved only
   because the range object carries a new `levered_names` key. No figure on any existing row changed.

   **The leverage flag the verdicts asked for now exists.** `leverage_debt_share_pct` is the bridge
   over the enterprise value, computed on every row of the new file, and `LEVERAGE_FLAG_PCT = 80`
   catches exactly the two names the verdict names: Claritev at 87.5 per cent and Skillsoft at 89.0.
   It SAYS something and does not DO anything: the founder gets a sentence naming the company and the
   percentage, and no median, band edge or exclusion moves. Evolent Health at 63.9 per cent is the
   third name a lower threshold would catch, and the threshold is Daniil's (rulings item 5).

2. **The third line is in the quiz.** Monthly revenue and the growth they plan give the
   next-twelve-months figure; it is shown as an editable line with the whole working printed under
   it; and an overwrite is what the field prices on. `index.html`, `app.js`. It is an AMOUNT, so it
   never leaves the browser: `ntm_revenue_exact` is named in check 15 as a refused figure, so the
   sentinel sweep proves it rather than the allowlist merely omitting it. The half that needs a
   ruling is unchanged and is rulings item 2.

3. **The engine-to-reveal wiring is finished, and it was not three small pieces.** Underneath it was
   a component that has been specified in `docs/engine-architecture.md` since 24 August and never
   built: **the profiler**. `reveal_payload.build()` takes a PROFILE; the 102 fixtures have profiles
   written by hand; a live founder has never had one at all. That is the real reason `api/reveal.js`
   answered `payload: null`.

   - **`selector/profiler.py`**: one model call turns the founder's answers and website into the ten
     tags the matcher reads. The vocabulary is COMPUTED FROM THE TAG FILES at import, so it cannot go
     stale the way the token-weight file did, and anything outside it is dropped and recorded rather
     than passed through. Product tags are fenced by shape (12 max, 60 characters, pipes and markup
     stripped). No number ever comes from the model.
   - **`api/payload.py`**: a Python serverless function beside the Node one, so the engine stays the
     single source of every number instead of being ported to JavaScript. The tier is the SERVER'S
     and a browser claiming `paid` still gets `free`, or rule E8 would be a suggestion.
   - **`reveal-client.js`** posts the same one body to both endpoints in parallel.
   - **`field.js`** rows 4, 5 and 6 now price on the engine's lanes instead of printing "in build".
     That closes the second source of truth: the football field and the peer charts now come from one
     selection run.
   - **`tools/render_harness.py`** draws all 102 fixtures on one contact sheet, both tiers, and
     asserts two things while it does it.
   - **Check 17**, `tools/check_profiler.py`, is the profiler's boundary: every case is a model
     misbehaving on purpose, including a hostile website telling it to output a valuation and print
     the key. The worst that escapes is a wrong comparable set.

**FOUR THINGS FOUND ON THE WAY, three of them older than today's work.**

1. **The stale-universe rule was global and quietly disarmed the whole universe.** `CURRENT_AS_OF`
   was the newest `as_of` anywhere, and any row not carrying it lost every multiple. That worked only
   because the 1-September refresh happened to touch all five peers files at once. A sixth file dated
   2026-09-05 made all 511 existing rows "left out of the latest refresh" and stripped their
   multiples. **The gate did not notice** (it counts names found, not prices shown) and reported 97
   of 102 while every range in the product was empty. Golden caught it. The comparison is now per
   source file, which is what "the latest refresh" always meant, and it marks the same 13 rows stale.
2. **The suite had TWO red checks since 6 September, not one.** Check 3 was failing on `n_estimates`
   and `revenue_growth_cy3_pct`. `n_estimates` is the broker count a coverage floor would use and is
   now read onto the row (nothing filters on it); `revenue_growth_cy3_pct` is explained beside its
   siblings.
3. **A third door on the free tier, and it is the same shape as the two found on 6 September.** The
   honesty caveats are written about the LEAD range, the lead range is the private one, and two of
   the sentences print a figure. So a free founder saw "At most 12.2x" one line under the blur, 12.2
   being the high of the lane they had not paid for. Check 14 walks every free payload for figures
   and missed it, because it reads numeric FIELDS and a caveat is a STRING. **The render harness
   found it on its first run, on fundraisly.** The caveat is still said, without the figure, because
   the lock is over the numbers and never over the honesty. Check 14 now reads the prose too, and
   putting the bug back makes it fail.
4. **Check 14 was testing a configuration the product can no longer be in.** It passed
   `raise_musd=3.0` and reported all 102 fixtures carrying an investor list. With no raise, which is
   what rule E9 guarantees, `RP.build()` returns ZERO callable houses for all 102, because **the
   fixtures carry no stage** and the stage gate is what replaced the raise on 6 September. The live
   path is fine (the profiler copies the founder's stated stage from step 1, and it produces 8 cards
   a founder). But nothing in the suite was exercising it. Check 14 and the harness now build the
   live shape. **Putting a real stage on each of the 102 fixtures is better and it is data somebody
   has to decide, so it is written up here rather than invented.**

**Fable, done 6 Sep: the matching refinement.** Daniil's 5-Sep to-do, run in full on 6 Sep. The
write-up is `docs/matching-refinement-6sep.md` (project: `claude/Fairway_matching_refinement_6Sep.md`)
and the stage-by-stage graphic `docs/matching-stages-6sep.html`. What it produced is in the log
entries of 6 Sep and in the "Closed 6 Sep" lines of the box below; what it left is in TAKEOVER at
the foot. The prompt it ran on is preserved as Appendix C of the write-up.

**Fable, carried:** read the recommendations strings for the first ten fixtures · page 3 to $ once
ruled · regression hover slope and intercept the day A1 lands.

**The bulk resolution of the No-comps list (search-enabled Claude session, one pass, after the 18-Sep
march and before 21 Sep):** now with `docs/sourcing-list-8sep.md` as its list (Tuum, Thought
Machine, Eurowag, Descartes, project44, Symbotic, AutoStore, STX with its caveat, specialty P&C
carriers, construction rounds, out-of-home media, and the rest), the A16 neighbour lookup run over
every fixture with a DIRECT name, and Daniil's own comparables when he provides them; plus the list as it stands after four marches, plus the loose ends from the
5-Sep pull that belong in the same pass: the "no source" rejections (HeyGen, Bolt.new, Zip and Altana
ARR, Highnote, Public.com, Petpooja, Norm Ai, Persona, Lighthouse, LTG's offer page, Logility, Blue
Yonder), Confluent and Verint, the eleven unreachable fund sites, and **the 13 gross rounds sitting in
lending archetypes**, which the net-and-gross change does not reach because the lending fork does not
ask for revenue at all. Nothing from this list is chased before then.

## (ii) Next week: 8 to 12 September

**Wiring week.** ~~Engine to reveal finished~~ **DONE 7 September, and it needed the profiler, which
had never been built.** The payload is served by `api/payload.py`, the page reads it, and the
football field prices on the engine's lanes instead of printing "in build". ~~The reveal render
harness~~ **DONE 7 September**: `tools/render_harness.py`, all 102 fixtures on one contact sheet,
both tiers, asserting that every caveat reaches the markup and no locked figure appears on a free
page. It found the free-tier caveat leak on its first run. The quiz walker extended to full branch enumeration as a nightly
check; the maths property tests extended to reveal arithmetic (midpoints, band construction,
dilution, bar geometry). The reviewer workflow (lead, banker review, commentary inside 24h, the
actual paid service) built and exercised once end to end. Legal placeholders off the live page the
moment the contact email lands; price on the page; invoice flow ready. The two payloads for the free
and paid tiers, once the lock ruling lands. **Marches 1 and 2 (8 and 11 Sep), 30 to 40 new test
companies each**, gate re-scored and the No-comps list grown after each; no data pull in between.
~~March 1~~ **DONE 8 September** (40 companies, gate 135 of 142, `docs/march-1-8sep.md`); march 2
on 11 September takes the next eight from each of the same five seeded lists, so it cannot draw a
company march 1 already took.
**Quiz testing starts this week** (it has not started), and the landing keeps its share of the time,
on Daniil's 23:10 ruling. Fable's matching refinement runs alongside. **Friday 12 Sep, 18:00 UK
checkpoint: dress rehearsal go/no-go, in writing.**

## (iii) Next month: 15 September to early October

**15 to 18 Sep: dress rehearsal.** Five to ten friendly founders end to end with real banker
reviews on the clock; fix list only; honesty sentences read in situ; analytics and the cookie
decision; outreach prep; final landing round; marches 3 and 4 (15 and 18 Sep, 30 to 40 new test
companies each); **then the one bulk resolution of the No-comps list, after the 18-Sep march and
before launch** (see TO DO (i)). **Week of 21 Sep: pilot live.** Daily triage; nothing new ships but
fixes. Fallback pre-committed: if the SM&CR answer has not landed, the pilot runs free with real
reviews and invoices when compliance clears; the date does not move. **Late September into October
(post-launch hardening, in rough order):** the taxonomy rule (ruled yes 6 Sep, measured at 29 of 102;
build after the pilot with the fallback carrying the founders it strips, and fold the near-duplicate
industry labels first); revenue-carrying fixtures (the basis/period machinery on real founder numbers); **the
listed-side basis tagging (513 rows, the known hole), which is what would let a founder's gross figure
meet listed comparables as well as private rounds**; the 58-round master file wired once its 14
source URLs land; the 192 listed CAGRs and the growth-band refit that follows; `asset_intensity`
wired into the ecommerce fork; the 101-row basis backlog burned down inside data touches; deck upload
feeding the banker review (the 1752-style phase 2); Stripe; VAT watch (120 engagements at £750).
**Post-pilot, from the strategy review, none of it before the pilot's numbers are in:** the "What
happened to companies like yours" panel (historical base rates by stage and cohort, every row sourced
and dated, founder-facing, no prediction; the honest form of the friend's third idea); the investor
read of the same reveal, scoped for Horizon 2; the Equidam check, quarterly, first in early December.
**1 October:** this document is archived and `Fairway_STATUS_2026-10.md` starts from scratch.

---

## Open decisions box (as of 9 Sep, 00:30 UK)

**New 9 Sep, from the evaluator's verdicts (Fable), needing Daniil:** rulings 9 to 14 in the TO DO
list above (hop-aero and ultrasonium; Buffer for publora; generic words in the score; derya's
reading; the three listed training-data vendors; when A16 runs). **Answered 8 Sep and applied:**
the six re-tags, Route A, generic words out of the closeness label, E1 (lyka) and E2 (measured,
rejected), cause 3 parked, the sourcing list, the Fundraisly task, rule A16.

**New 7 Sep, from the landing page 3 build (Fable), needing Daniil:**

- **The example's peer multiples and round dates are not the engine's.** The landing says HubSpot
  10.3x, Klaviyo 9.9x, Braze 9.4x "as of 27 Aug 2026"; the listed pool (1 Sep refresh) holds 2.8x,
  3.0x, 3.4x on EV to next-twelve-months revenue; the page's Vanta round is Jul-25, the pool's is
  Jul-24. Known state of the example (item A1), and page 2's field is built on the same figures, so
  page 3 was kept consistent with page 2. Ruling needed before the pilot: a real run replaces the
  example, or the "as of" stamp goes.
- **The peer count is aligned across the three pages, except one row.** Hero: "5 to 7 peers" (the
  ruling, not an example count). Page 2: the revenue row says "6 core peers" and its band, 8.2x to
  10.3x, is exactly the six named, so no arithmetic moved; "Our read" lists the six with no "+6
  more". Page 3: six. **The one row not aligned is the gross-profit row on page 2**: its band, 9.5x
  to 12.0x, rests on six names of which two (Pegasystems, Intapp) are not in the set of six, and
  it still says "+6 more inside the band". Aligning it means either a gross-profit multiple for
  Sprout Social and Appian, which the example does not carry and no session may invent, or a real
  run (item A1). Ruling needed; A1 is the recommendation, and it also settles the item above.

**ALL FOUR OF THE OLD RULINGS ARE CLOSED, 7 September.** The per-user chart width closed without a
number, because removing the enterprise-contract readings at source took the widest chart from
1,034x to 97x. The next-twelve-months question is answered and built: the trailing figure stands in,
on both readings, with a warning about the mismatch. The raise closed with no compromise: the cheque
filter moved into the browser and gives the identical list on 102 of 102 fixtures. Check 15 without
node is answered fail, which is what the code already did. Nothing from that list is waiting on him.

**New 7 Sep, from wiring the forks, needing Daniil:**

- ~~Three archetypes belong to two forks and route on dict order.~~ **CLOSED 7 Sep by the ruling
  that both figures are optional.** Consumer & Prosumer Software goes to the software fork, which
  asks ARR and subscribers and prices whichever arrives, so the order no longer decides anything a
  founder would notice. Streaming and Dating go to the merged `consumer` fork. The media fork, which
  could never fire, is gone. **What is left is a fixture gap, not a ruling:** no test company
  carries Streaming & Digital Media or Dating & Social Network, so the consumer fork is still
  unwalked by check 10. The marches should pick one of each up.

**Open, needing Daniil:** the two from the landing page 3 build above, which have ONE answer between
them (item A1: a real run replaces the example, now possible for the first time because the engine
is wired end to end as of today), and one from the data, his to fix when he gets to it:

- **CAB Payments' gross profit** is negative in the file (-$15m on $177m of revenue) and does not
  look right. Parked for Daniil to fix later (his words, 6 Sep).

**New 7 Sep, 19:50 UK, from the strategy review (Fable), both optional:** rulings 7 and 8 in the list
above (a survey question after the reveal; a tally of introduction requests). Neither changes the
launch if left unruled. The three horizons themselves are agreed and stand at the top of this document.

**Answered 6 Sep, late (Daniil's fifteen), and what was done with each:**

1. The ceiling rule applies to per-user counts too: a count quoted as "more than" is a bound,
   never a reason to exclude. Recorded; the release scripts already work row by row, so a released
   row's count prices too.
2. ultrasonium is a high-end manufacturer selling to Aerospace & Defence, Compute & Data Centres,
   Energy Systems and Robotics. End market set to Manufacturing (the one label those customers
   share) and the four markets carried as product words; it now passes on PTC, Dassault Systemes
   and Lectra with Xometry in the wider ring, and check 8 records that lane as resting on the end
   market alone (kind 3). The vocabulary has no Aerospace & Defence label; noted for the taxonomy
   build. Gate 96 to 97.
3. Factorial: the field said GROSS and the row's own note said NET (a subscription HR software
   business keeps its turnover). Field aligned to the note.
4. Monthly revenue times growth gives the next-twelve-months figure, shown as an editable third
   line with a hover: Opus to-do 2 above. The open half (growth not given) is rulings item 2.
5. "Raise band" means the amount the founder is raising. Restated as rulings item 3.
6. `deal_note` is the cited-deal sentence on a founder's investor card. It is not empty; six rows
   had a stale sentence on the end ("STAGE BAND LEFT EMPTY") from before the bands were filled.
   The stale sentence is removed, the cited deal stays (`tools/apply_investor_fixes_6sep.py`,
   532 rows in, 532 out, 7 edits). Found on the way: Y Combinator's band read "Growth; Late stage;
   Crossover" against its own row's $125,000 SAFE; set to "All stages", the phrase its page uses.
7. Check 15 and node: restated as rulings item 4.
8. Contact email: left empty for now.
9. The whole landing in dollars: done. Every pound figure in the investor notes converted at the
   page-2 rate (1.35 $/£, 4 Sep 2026), 41 pound signs to 1 (the currency symbol table), and a
   one-line rate note under the investor card.
10. Episode 1: the website's figure ($340k to $2.7M at that rate) on both notes.
11. Edenred and Pluxee: multiples stand as in Daniil's file (0.8x and 2.8x, Bloomberg agrees);
    P/E correct. Closed.
12. CAB Payments: parked (above).
13. unifold: the 5-Sep pull prompt named it among the four hardest sourcing gaps but the prompt's
    gap table has no row for it, so nobody holds a brief for what to look for. Moved to the bulk
    pass list (TAKEOVER 10). No decision needed.
14. Kriya, Vegrow, Fireblocks Jan-22: confirmed from the rows. Kriya is out of the medians because
    its numerator is the accounting purchase consideration Allica Bank recognised, not a valuation
    ("not clean EV"); Vegrow is in the medians at 6.68x with a founder-facing caveat on its tier-4
    source; Fireblocks Jan-22 is in at 80x. Closed.
15. Accel above seed specialists: two things done. The ranking inside a tier now puts the house
    whose band covers fewer stages first (a seed-only fund above a seed-to-Series-C house), before
    deal count. And the Accel row was two things in one: the callable card was the Accel Atoms
    pre-seed programme for Indian founders (cheque $1m to $2m, atoms.accel.com) ranking on the core
    funds' 20 rounds (Legora and Decagon Series D, median post-money $5bn). Split into Accel
    (evidence only) and Accel Atoms (callable, on its own evidence, which is none yet, so it does
    not render until an Atoms deal is sourced). marble's list is now Air Street, Connect, Episode 1,
    Notion Capital, Playfair, Seedcamp. 86 fixtures rebaselined on these reasons.

**PARKED, on Daniil's word 6 Sep:** the **SM&CR / outside-business-interest** answer. It does not
gate the pilot: the fallback already written in stands, so the pilot runs and invoices when
compliance clears, and the date does not move.

**Closed 6 Sep (23:35 UK, Daniil's fifteen answers):** the ceiling rule applies to counts too · ultrasonium's
end market is Manufacturing (four customer markets named) · Factorial is net · the landing is all
in dollars · Episode 1 at the website's range · Edenred and Pluxee stand · Kriya, Vegrow and
Fireblocks confirmed · contact email stays empty for now · stage specialists outrank generalists
and Accel is split from Accel Atoms · the next-twelve-months line is specified (Opus to-do 2).

**Closed 6 Sep (22:50 UK, Fable, on Daniil's late messages):** **the taxonomy rule is RULED YES**
(measured 29 of 102 change, gate 95 to 94; build after the pilot; a founder it strips of label-only
names is carried by the recorded fallback, which is his edviro instruction) · **Wix** is Consumer &
Prosumer Software first, Business Applications second, function Productivity ("a website builder,
just software"); 9 fixtures' listed lanes move · **Owner's** Marketing secondary dropped (3 private
lanes lose it) · **AuditBoard** to Business Applications first (0 move) · **apollo-atomics** keeps
its tag · the "served on a generic word" kind on the No-comps list is agreed: a founder is served,
the depth of similarity is limited, and the list says so.

**Closed 6 Sep (late evening, Fable):** the ceiling ruling applies throughout (10 rows released, 2 held
for a named other reason) · the "also compare with" list built as `also_compare` on the founder
profile, rule A2 in reverse, with Vention and Applied Intuition pinned for manifold-robotics and
Whatnot and StockX for tash, gate 94 to 96 · BVNK is Crypto & Digital Assets first and Cross-Border
& FX second · the "293" explained: 294 of 1,117 comparable slots on weak evidence, 3 lanes resting
on nothing else, printed by check 8.

**Closed 6 Sep (evening, Fable):** the token-weight file (regenerated by Daniil in `5e518d2`, and check 16
now fails the suite if it goes stale again) · StockX's ceiling released into the gross medians (a
"more than" figure is a ceiling, never an exclusion) · BVNK is infrastructure first (Crypto & Digital
Assets primary) · the No-comps list has a third kind of entry, served on a generic word or the end
market alone, recorded not removed.

**Closed 6 Sep:** what a founder who gives no numbers sees (up to four peer bar charts, and the
per-user one now reaches 54 fixtures instead of 6) · the free tier (public free and complete,
everything else blurred, names and dates on hover, no figures in the payload; rule E8 amended) ·
the pricing tiers (free is the 24-hour banker read; $250 the full field with private tiers and DCF;
$750 the valuation presentation; nothing contradicts) · the growth definition (CY+1 is the current
year, so CY+1 to CY+3 is the same window as CY+0 to CY+2, tested 16 of 16) · `investor_check` versus
the cheque ruling · Udemy (released, and the figure we held was already his method) ·
`basis_compatible` (deleted).

**Closed 5 Sep:** the relevance gate (A12: failure flagged, fallback as a recorded rescue) · **what
the four dates are for** (23:10 UK: marches of 30 to 40 test companies, not data pulls; the No-comps
list resolved in bulk once before launch; matching and tagging refined in the meantime) · **the name**
(No-comps list, not "register") · **net and gross both** (23:35 UK: ruled, and BUILT the same night as
rulebook B3a; gate 90 to 95) · **three of the four held rows** (Moss, Restaurant365, AuditBoard
released in `6433348`) · the float adjustment (none; Daniil's figures stand) · the currency of the two
Euronext rows (USD, rate derived) · the empty-lane question (ruled 4 Sep and implemented) · rulebook
C7 (a listed comparable has to still be listed).

**Closed 4 Sep:** the negative multiple (n.m.; XP and CAB repriced on a zero bridge) · the investor
sector vocabulary (translated at read time) · the currency of the 750 (USD) · the entity (none, sole
trader) · the address for service (29 Westbourne Terrace, London W2 3UN) · the price (three tiers).

## Session log, newest first, date and time always

- **9 Sep, 00:30 UK (Fable; the session ran from 20:00 UK on 8 Sep)**. **The evaluator's 142
  verdicts checked one by one, then Daniil's answers applied.** First the review, nothing changed:
  every flag walked through the engine's fences with the words and tags that produced it, every
  "better name in the pool" looked up and the fence that stops it named, every re-tag measured in
  memory. Of 13 flags: 6 fair, 3 fair with the wrong cause, 2 partly fair, 2 pushed back (emergent:
  Lovable and Replit were in its private lane all along; derya: a broker by its own page). The
  evaluator saw only the pricing set, not the wider ring, and applied a stricter bar than Daniil's
  8 Sep rule. Two engine findings of my own: the closeness label counted "a", "as", "ai" and "data"
  as shared descriptors (69 of 116 strong lanes rested on under one point of evidence), and lyka's
  BARK sat in the wider ring on an end-market spelling. Then his answers, in one batch: six
  fixtures re-tagged (fundraisly, alloovium, kita, lambda-robotics, supercritical, lyka; florin and
  princeps to the Insurance end market), `Supply Chain & Logistics Software` created in the
  software family (`tools/apply_supply_chain_software_8sep.py`, 9 rows, count in equals count out
  on all three files), the end-market folds (`tools/apply_industry_folds_8sep.py`, 10 rows, 204 in
  204 out), eighteen stopwords and the closeness label on non-generic words (`match_reference.py`;
  weight file 405 in, 400 out, a, as, in, on, to dropped and named; check 16 green), gate rule 4
  narrowed (`peer_universe_check.py`), the profiler's four rules with check 17 asserting them,
  rules A13 to A16 in `docs/RULES.md`, the fixture table regenerated, Samsara pinned for
  ekho-labs. **Suite in a fresh clone of `41d64fa` holding the batch: twenty checks, all pass;
  gate 130 of 142 (fundraisly, lyka, princeps in; ekho-labs, lambda-robotics, supercritical out,
  each for a written reason); golden rebaselined 130 of 142, attributed line by line in
  `docs/golden-attribution-8sep.txt`.** E1 measured at zero fixtures moved; E2 measured two ways
  and rejected with the names it let through. **Things that touch his instructions, said to him
  in the chat at the time:** Lovable and Replit already in emergent's set; lyka's BARK was a
  tagging error; hop-aero and ultrasonium left untouched for want of an answer; derya tagged his
  way with the page's reading recorded; A16 scheduled inside the bulk pass under A12 part 3; A14
  applied to the label and not to the score, with the 6 Sep measurement as the reason; Appen,
  Innodata and TELUS Digital put on the sourcing list against his reading that no listed
  training-data vendor exists. Files: the TO COMMIT block. Documents: `docs/evaluator-verdicts-review-8sep.md`
  (project: `claude/Fairway_evaluator_verdicts_review_8Sep.md`), `docs/sourcing-list-8sep.md`,
  `docs/golden-attribution-8sep.txt`, this document.

- **8 Sep, 20:15 UK (Fable)**. **The field drawn small on the hero, growing into page 2 on the
  scroll.** Daniil on the four-page phone: "This is much better." Then: could the football field
  sit on the first page, even smaller than on the second, and expand as the user scrolls down?
  **Built:** a slot at the foot of the hero copy, above the three facts, sized by `showcase.js` to
  44% of the page-2 card (143 by 148px on an iPhone 13). The page-2 card itself is drawn into it by
  a transform (it never leaves page 2's flow), and as the scroll moves from page 1 to page 2 the
  transform runs from that slot to the card's own place: it glides up and grows, 198px wide at
  30% of the way, 253px at 60%, 326px on arrival. One element, so the small one and the big one
  cannot disagree. Tapping the small one scrolls to page 2. Under prefers-reduced-motion the card
  simply sits on page 2. The card's opening animation on phones may only fade it in, never move it,
  or it would jump into the slot after the fade. Page 1 still fits its screen exactly (608 of 608
  inside the copy) after trimming the hero's margins. **Verified:** rendered at rest, at two held
  mid-scroll frames (snapping held off for the test only) and on page 2; no script errors; desktop
  and tablet pixel-identical to `4e080f4`; suite twenty checks, all pass, golden 0 of 142, gate 130.
  **Live as of 19:54 UK:** the four-page phone (`d1809b1`); this batch adds the small field on the
  hero.
  **Rule for this document, after tonight** (three sessions, one file, one hour, two overwrites):
  stage it from the laptop immediately before writing it, write with the change guard on, and if
  origin has moved since the copy you started from, merge from origin first; a commit block that
  says "ONE batch" when the tree holds three has cost a session's record twice tonight.

- **8 Sep, 19:40 UK (Fable)**. **Daniil's reply to march 1, applied: a set a person strikes fails
  the gate; five struck; the three stages looked up; a table of all 142 companies beside their
  comparables for him to read one by one.** His words on seeing Orca Aerospace, Constellation
  Space and Zymbly pass the gate on Samsara, Owner, Guesty and Restaurant365: "such companies should
  immediately go into the no-comps list. Now it makes me wonder if there are more issues that I'm
  not aware of." He was right that "on the list" and "passing the gate" had been allowed to mean
  two different things. **Built:** a fixture can carry `struck` (the reason, in words, by whoever
  read the set); it FAILS the gate (rule 5 in `tools/peer_universe_check.py`) and check 8 prints it
  as **kind 4** of the No-comps list with what it was shown. Struck now: orca-aerospace,
  constellation-space, zymbly (aerospace and aviation on restaurant and hospitality rounds),
  vorelios (engineering simulation on frontier-lab rounds), daivin (dive gear on CAD software).
  **Gate 135 to 130 of 142.** Rule A12 part 3 now names all four kinds. **His questions answered
  in the march document, section 5:** the aerospace, hardware and simulation fixtures are on the
  list as kind 4 (and kind 1 where the label fallback fired); Ornadyne and PRINCEPS as kind 2;
  training-data vendors and government affairs have no listed names by his reading, so nothing is
  owed there; Quippy reaching Duolingo agreed. **The table:** `tools/fixture_comparables_table.py`
  writes `docs/fixture-comparables-8sep.md` and a `.csv` with an empty VERDICT column, every one of
  the 142 beside the listed and private names the engine chose, how close it judged each lane and
  how many names carry a price; rerun after every march. **The three stages** (his instruction:
  "check online which stage makes sense; if nothing surfaces, leave blank"): TwelveLabs Series B
  ($100m, its own press release, 1 Jul 2026); Airtop Series A ($25m, TechCrunch, 27 Jul 2022, as
  Switchboard; nothing later surfaced); World Labs a $1bn round of 18 Feb 2026 that no source
  names by letter, written as Series C because that is the engine's own top band for a round of
  that size, marked DERIVED in the code for him to blank. **Golden:** exactly those three moved,
  and only their investor lists (Andreessen Horowitz leads at Series B and C, Cherry Ventures at Series A); the
  comparables did not move; rebaselined on that reason, 0 of 142 after. **A matcher finding for
  the tagging work between marches:** the words that carried the aerospace passes were
  "operations", "software", "mission", "management" and "recovery", none on the generic list.
  **Suite:** nineteen checks, all pass, in the clone; gate reproduced on his Mac. Files: as in the
  TO COMMIT block.

- **8 Sep, 19:20 UK (Fable)**. **The phone as four snapping screens; the field is a small picture;
  what was live was never the redesign.** Daniil opened the live site on his iPhone 13 at 18:22 UK
  and sent screenshots: the field "cramped", "impossible to read", "doesn't even look like a football
  field". **What he was looking at was `23ef743`, round one of the third pass, the version he had
  already rejected at 19:45 UK on 7 Sep.** Round two (the field as the desktop hero card at phone
  size) never reached his repo: the link to his Mac dropped while it was being written, it went into
  the chat as a zip, and the zip was never applied. That was said to him plainly; the lesson for this
  document is at the foot of the entry.
  **His brief, in his words:** the field "somewhat smaller", "almost like a photo", "not the whole
  width and not the whole height, some share of the screen, so that you have the explanatory text
  visible along with it"; per row "unrefined range, the figures, and then the bar"; the privacy
  paragraph under the button "should not be there by default", an "i" button; "three or four
  distinct scrolls, once you scroll a new page appears, you are not supposed to be stuck somewhere in
  between"; the small print "occupies too much space"; the strip visibly restarts.
  **Built, and rendered on an iPhone 13 profile at 390 by 664:** four pages, each exactly one screen,
  and the scroll SNAPS between them (CSS scroll snap on the root, `scroll-snap-stop: always`, so a
  flick moves one page and a short scroll falls back; measured with wheel events: rests only at 0,
  664, 1328 and 1992, never between). (1) the hero, the copy centred under the nav, "About four
  minutes · no card" with an "i" ring that opens the privacy sentence in a grey panel; (2) "One
  valuation, argued multiple ways", the field as a picture 92% wide and 336px tall, one line per row:
  the method, the two figures with a dash between them, the bar on a shared axis, the +53%, the
  axis; under it the five things Fairway does as coloured one-liners, then the button; (3) Under the
  hood: the four rules as first lines only, the funnels side by side, the four tap-to-open rows;
  (4) the close: the reviewing-team strip on top, the button in the middle, the two paragraphs of
  small print folded behind "Your data" and "About this service". `showcase.js` does the phone-only
  DOM moves (story block above the card, steps below it, the "i", the last page and its folds);
  `landing-mobile.css` is rewritten from scratch; `index.html` is untouched, so check 18 reads the
  same page.
  **The strip's restart was a real bug, on every screen size.** The list is repeated six times and
  slid by half the track's width, but the 38px gap after the last name was missing, so every loop
  landed 19px short and clicked. One line of `padding-right` in `shared.css` fixes it; the desktop's
  strip now differs from `63bb628` by that 19px per loop and by nothing else.
  **Rule touched, said in the chat:** the privacy sentence (6 Sep: said BEFORE the quiz) is one tap
  away on the phone's hero; the quiz screen still shows it in full above question one, so the rule
  holds. **Off the phone, each one line to reverse:** the multiples and metric columns, the grey
  sub-labels, the six callouts, Our read, the hover pop-ups, the rest-state strip, the rules' second
  lines, the last-round marker row.
  **Verified:** every page 390 of 390 wide, no script errors; the "i", the four rows and the two
  folds open and close with `aria-expanded`; desktop at 1440 by 900 and tablet at 768 pixel-identical
  to `63bb628` at every scroll position tried, except the strip (the 19px) and the scroll cue
  mid-animation; suite in a fresh clone of `63bb628` holding this batch AND the march batch below,
  twenty checks, all pass, golden 0 of 142, gate 135 of 142. **Still not looked at on a phone: the
  reveal and the quiz's fork step.**
  **Three lessons for the next session that writes over the bridge.** (1) When the link drops
  mid-delivery, say in the first line of the reply that NOTHING reached the repo, and say it again
  in the commit block; a zip in the chat is not delivery. (2) Check origin before writing this
  document: the march session below wrote its copy from `91c3d7a` while origin was at `63bb628`,
  and its copy had silently dropped the fork entry of 7 Sep 20:30. (3) Stage this document from the
  laptop immediately before writing it, and write with the change guard on: three sessions wrote it
  within one hour tonight and each of the first two silently lost the other's entry. This copy was
  merged by hand from all three, and the suite was run on all three batches together.

- **8 Sep, 18:50 UK (Fable, on the scheduled run)**. **March 1 of four: 40 new test companies,
  fixtures 102 to 142, gate 135 of 142, the No-comps list grown on all three kinds.** Full record
  in `docs/march-1-8sep.md` (project: `claude/Fairway_march_1_8Sep.md`): every company with its
  page, its website, what it sells in one sentence and its stage where stated.
  **How they were chosen, so march 2 does it the same way:** the YC directory pages for the Fall,
  Summer, Spring and Winter 2026 batches and the Product Hunt weekly leaderboard (week of 31 Aug)
  read in Chrome; every company not already in the 102 went into a pool (36, 68, 55, 59, 29);
  eight drawn from each by a seeded random draw (seed 20260908). Left out before the draw: the 19
  already in the set, three model or feature releases by OpenAI and Google, one advert, and two
  YC companies that also appeared on Product Hunt (kept once). 32 read off ycombinator.com, 8 off
  producthunt.com; the label is the company's own tagline; nothing invented, nothing substituted.
  **Stage:** no page states one, so all forty carry the Seed default as a test condition; World
  Labs, TwelveLabs and Airtop are visibly later-stage and are put to Daniil (the one decision).
  Two are hardware outside the market (Ornadyne, DAIVIN!) and go into OUT_OF_MARKET, kept not
  dropped. **The gate:** 38 of 40 pass; ornadyne and princeps fail cleanly (no priced public lane).
  **The No-comps list after the march:** kind 1, 10 lanes served on a label, 52 comparables (2 and 8
  before); kind 2, 7 of 142 not served at all (5 of 102); kind 3, 428 of 1,570 comparable slots on a
  generic word or the end market alone (302 of 1,120), lanes resting on nothing else 4 to 8.
  **What the march found, read by a person** (section 5 of the march document): aerospace, space and
  aviation have no home in the pool or the vocabulary (Orca Aerospace, Constellation Space and
  Zymbly pass on Samsara, restaurant and hospitality rounds; TAKEOVER 11 gains three names);
  Vertical Software with a Horizontal end market hands Owner, Guesty and Restaurant365 to five of
  the new fixtures, which is the taxonomy rule's case (ruled yes, build after the pilot); hardware
  keeps getting design software (A11 again); AI training-data vendors (Markov, Praxis, Qokedas)
  have a good private lane and no listed one; engineering simulation (Vorelios) and compute
  insurance (PRINCEPS) are absent; Fed10's obvious listed peer is not in the pool. Where the pool
  is deep the sets are good: Playabl gets Roblox, Epic and Patreon; RASPIRE gets SentinelOne,
  Zscaler, Wiz and Snyk; myAIcademy gets Duolingo, Coursera and Udemy. **One re-tag on the way,
  and it is a lesson for the profiler:** Quippy tagged consumer software first got F-Secure and
  Opera on the word "app"; tagged Online Learning first, the way wondering is, it gets Duolingo,
  Coursera and Nerdy. **None of the gaps is chased** (A12 part 3); they are written into the march
  document for the bulk pass. **Golden:** run before writing, 102 ok, 40 missing, 0 moved; then
  `--write`, 142 files, 0 of 142 moved. **Suite:** nineteen checks, all pass, in a fresh clone of
  `91c3d7a`; check 12 reports 7.9 houses a founder and one fixture with fewer than three
  (ornadyne, 1). Files: `selector/golden_profiles.py` (REAL_4 block, OUT_OF_MARKET extended),
  40 new `selector/golden/*.json`, `docs/march-1-8sep.md`, this document.

- **7 Sep, 21:40 UK (Opus)**. **Daniil's ruling on the consumer fork, and it settled more than the
  question asked.** His words: "Both revenue and user count should be optional for them. If they
  give both, we price them on both. If they give either one of them, we show the one that they gave.
  If they give nothing, we only show the bar charts with multiples, not football fields."
  **ONE CONSUMER FORK INSTEAD OF TWO THAT COULD NOT BOTH EXIST.** `consumer_subscription` and
  `media` claimed overlapping archetypes and the first one in the file won all of them, so **the
  media fork could never fire from the day it was written** and the quiz walker has said so on every
  run. They also disagreed about the founder: one required a subscriber count and made revenue
  optional, the other the reverse. Measured against our own comparables, requiring the count was
  backwards (Dating & Social Network: 4 comparables, 4 price on revenue, ZERO on a count, and no
  listed company in any of the three carries a user count at all). Merged into `consumer`, every
  figure optional. Nine forks to eight, and none of them dead.
  **THE RULING IS GENERAL, NOT LOCAL, and it had to be.** He ruled on 6 September that a founder who
  gives nothing still gets a reveal. Every one of the nine forks had a REQUIRED money question, and
  **when I put the forks on the page earlier tonight that flag became a button a founder could not
  get past.** The fork step was stricter than the plain revenue question it replaced, which has had
  a "We are pre-revenue" escape since it was written. That was a regression I shipped and this
  removes it: no figure is required in any fork, the page never disables Continue over a number, and
  the two choice questions (the lender's funding model, the exchange's unit) still gate because they
  are labels rather than amounts. With no figures at all the football field is hidden entirely and
  the founder gets the peer charts, which is his ruling exactly.
  **TWO MORE DEAD WIRES, AND ONE WAS THE SOFTWARE FORK'S OWN FIRST QUESTION.** Each basis names the
  profile field it multiplies; each question names the field its answer lands on; the two had
  drifted. **`arr` landed on `revenue` while the ARR basis read `arr`**, so the question 57 of the
  102 test companies are asked first reached nothing. Every subscriber count landed on
  `paying_subscribers` while the basis read `subscribers`. Both fixed, and `arr` no longer pours into
  `revenue` at all, which is the same trailing-versus-forward distinction the page has made since
  6 September. **Check 20 now asserts every fork answer reaches the basis that prices it**, which is
  one level deeper than check 10 has ever looked: check 10 asks whether an answer LANDS, never
  whether the basis can SEE it.
  **NO FURTHER RULING NEEDED ON THE SHARED ARCHETYPES.** Consumer & Prosumer Software goes to the
  software fork, which asks ARR and paying subscribers and now makes both optional and prices both,
  so it already does what he ruled. Streaming and Dating go to `consumer`, which fires. What is
  missing there is a TEST COMPANY: 0 of the 102 carry either archetype although we hold 18
  comparables for them. The marches will fill it.
  Suite: twenty checks, all green; golden 0 of 102; priceable 8 of 15.

- **7 Sep, 20:30 UK (Opus)**. **THE NINE FORKS REACH THE PAGE. They never had.**
  Daniil, on being told: "I do not understand how it is possible that the forks are not implemented
  ... this is just ridiculous because we did so much work on this, and I don't even know if we have
  all the notes stored."
  **The notes are not lost and nothing had to be rebuilt.** `selector/quiz_fork.py` has held the
  complete specification since 31 August: nine forks, every question with its label, its kind,
  whether it is required, which measure the answer lands on, the routing rule and the answer map.
  Check 10 has walked all nine on every run since. What did not exist was the other half: the PAGE.
  `index.html` asked the same nine fixed steps of everyone and collected one figure, current monthly
  revenue. Check 10 asked "does the ENGINE handle every fork", which was true. Nobody asked "does
  the PAGE draw them", which was false, for six weeks.
  **WHAT IT COST, measured on the 102:** 45 of 102 founders would be asked a different set of
  questions by their own fork; 5 are priced on something the live quiz never asks for at all. A
  lender was asked for revenue while the engine prices them on their book, which is the exact
  failure `fork_for` was written to prevent. The gross-revenue question ruled on 5 September, which
  unlocked 105 rounds, reached nobody.
  **BUILT TODAY.** `selector/site_text.py` fetches the founder's website (one request, three
  seconds, 400 KB, PUBLIC HOSTS ONLY with every resolved IP checked, markup stripped) which nothing
  had ever done, so the profiler has been running on dropdown answers alone since it was written
  this morning. `api/profile.py` is what the page calls after step 2: it runs the profiler, picks
  the fork and returns that fork's questions, never a figure in either direction.
  `quiz-fork.js` draws them, shows the founder what we read their site as, and lets them say it is
  wrong. `reveal-figures.js` prices what they answer.
  **PRICEABLE 1 OF 15 BASES TO 8 OF 15.** That is the measurement that says the forks landed: ARR,
  REVENUE, REVENUE_GROSS, BOOK, ORIGINATIONS, THROUGHPUT, PAYING_SUBSCRIBERS, BORROWERS.
  **CHECK 20** walks the whole path for all nine: routes, returns questions, the page draws every
  one, every answer lands on a field the engine reads, and the browser can price what each fork
  asks. It found three real bugs on its first run.
  **THREE BUGS IT FOUND, one of them older than today.** (1) The two choice questions in the whole
  product, the exchange fork's unit and the lending fork's funding model, arrived with no answers to
  pick from: the spec calls them `choices` and the endpoint asked for `options`. They could not be
  drawn at all. (2) **`throughput_unit` and `throughput_volume` never landed on the profile.** The
  exchange fork has asked for them since 31 August and `apply_answers` did not carry them, so a
  founder could answer "tonnes of CO2 equivalent" and the engine would never know the unit. Five
  lending fields had the same hole. (3) Three archetypes sit in two forks' lists and route on dict
  order alone: Consumer & Prosumer Software, Streaming & Digital Media, Dating & Social Network.
  Recorded and printed by check 20 rather than guessed at. **RULING NEEDED** (below).
  **IT DEGRADES TO YESTERDAY.** If the profile call is slow, refused or missing, the founder gets
  the plain revenue question and the quiz is exactly what it was. Nothing holds a founder up.
  Suite: twenty checks, all green; golden 0 of 102.

- **7 Sep, 19:50 UK (Fable)**. **The strategy review, the three horizons agreed, and Equidam read
  properly. Nothing in the launch changes.** Daniil brought three ideas from a friend (angels as the
  recurring audience; a curated deal-flow gate growing into an index with a syndication fee; a
  probability of success) and three questions (does it exist; change now or later; who to talk to).
  Researched with sources (UKBAA, British Business Bank, HMRC, ACA, Carta, Crunchbase, the FCA
  Handbook, SyndicateRoom, Odin, Hustle Fund, AngelList's own return studies), the load-bearing facts
  checked on their primary pages, written up as `claude/Fairway_strategy_review_7Sep.md`. The short
  answer: each piece exists (Angel Squad, AngelList and Odin, Access EIS, the SEIS volume funds,
  Crunchbase and PitchBook predictions), nothing bundles them, and the reason is licences and deal
  flow rather than ideas; angels are a recurring audience for screening, not valuation, and most do
  not set the price; the gate is arranging under the FCA rules and the index is fund management, so
  a 2028 destination through an authorised partner; a single probability of success cannot meet the
  rule that every statistic is real, and the honest form is a sourced base-rate panel. Daniil agreed
  the three horizons (founder utility first, investor read second, channel through a partner third)
  and asked for them in this document: they stand at the top, with the gates. **Equidam** is the one
  direct competitor on his reading (Gust, Seraf and SeedLegals are not valuation tools); read from
  its own pages and written up as `claude/Fairway_competitor_Equidam_7Sep.md`: six people, about
  2,000 paid valuations a year, five blended methods on the founder's projections and industry
  averages, a founder-picked comparables feature since December 2025, an investor line already
  selling. **Two things found in this document on the way:** Opus's 18:56 UK patch (`97c90b5`, the
  four rulings closed) was on origin and on the laptop but not in the project copy, and is now in
  both; and rulings 1, 2, 4, 5 and 6 still read as open in the TO DO list while the 17:40 and 19:20
  entries and that patch had closed them, so they are marked closed with their reasons. Two optional
  rulings added (7 and 8). No code, no data, no suite run: this session touched documents only.
  Files: this document (repo and project) and the two project documents named.

- **7 Sep, 16:35 UK (Fable)**. **The phone shows the same three pages as the desktop.** Daniil's
  ruling on the first phone pass (pushed in `d378bb1`): one continuous page is wrong; keep the
  desktop's format, the three pages arriving one after another, sized for an iPhone. So the genie
  (the name in the code for page 2's move: the hero pins, the copy gives way and the football field
  grows to fill the page as the reader scrolls) now plays on phones too. `showcase.js` turns it on at
  640px and below; tablets between 641 and 899px keep the continuous layout; desktop is untouched.
  On a phone the copy and the card stack, so the move differs from the desktop's: the copy fades and
  the card rises from under it to sit beneath the story block (the "One valuation, argued multiple
  ways" title and its five steps), which is compact on a phone so the card's head is on the screen
  when the move ends; from there the pinned content is shifted up with the scroll (`--card-shift`,
  set by the script) so the expanded field scrolls through like normal text, and page 3 follows.
  Three things measured and fixed on the way: the shift now stops where the hero stops sticking
  (before, the content moved twice and left a 318px gap before page 3); the distance the move plays
  over is fixed at the first measurement, so a phone's toolbar growing and shrinking the viewport
  cannot make it jump; and the two figures at the ends of each bar sit outside the bar, because on a
  324px track a bar is 35px wide and the figures ran into each other ("6.48.0"). The hover hint on
  the card is hidden on phones. Rendered on an iPhone 13 profile at rest, three points mid-move, the
  end of the move, three points down the field, the top, middle and foot of page 3, and back to the
  top: width 390 of 390 throughout, no script errors, the move reverses. Desktop at 1440x900: section
  height 3020px as before, page 3 cards end at 840px; tablet at 768px unchanged. Suite: nineteen
  checks, all pass. Files: `showcase.js`, `landing-mobile.css`, this document. **Still not looked at
  on a phone: the reveal itself** (`#screen-result`, needs `/api/payload`).

- **7 Sep, 15:50 UK (Fable)**. **Landing v0.1, the pilot version, is on origin as `3875c52` (Daniil,
  15:31 UK). Then the phone.** Daniil opened it on an iPhone and it did not open properly. Measured
  on an iPhone 13 profile (390px): the hero card, the football field, could not shrink below 480px,
  so the whole page was 463px wide, the hero copy sat 73px off the left edge, and the nav wrapped
  into three lines; page 3 never appeared at all, because `landing-p3.js` waited for a quarter of
  the section to be in view and on a phone the section is three screens tall. **Fixed:**
  `landing-mobile.css`, one media query at 640px and nothing above it (the nav keeps the brand and
  the button, the hero is one column, the field card shrinks and its labels wrap, page 2's blocks
  stack, page 3 is one column with the button under the title); the reveal threshold in
  `landing-p3.js` is 2 per cent. Rendered after: page width 390 of 390 on every screen including the
  quiz, no script errors, desktop unchanged (page 3 cards still end at 840px of 900). Also caught:
  the field's "EV / NTM revenue · 12 named peers" label, the last 12 on the page; now 6. **Not
  looked at on a phone: the reveal itself** (`#screen-result`, drawn by `field.js` and
  `reveal-client.js`), which needs `/api/payload` and cannot be rendered offline. Next session with
  the site live: open a reveal on an iPhone and check the field and the investor cards there.
  *(The Opus entry below is stamped 19:20 UK; its files landed on the laptop at 15:37 UK by the
  clock, six minutes after the v0.1 push.)*

- **7 Sep, 19:20 UK (Opus)**. **The raise question closed, and the answer was that the question was
  wrong.** Daniil: "can we not do the work of refining the universe of potential investors without me
  actually seeing what amount they are raising? How is it possible we are able to build a football
  field for them without me seeing the revenue figure, but cannot figure out the investor universe
  without raise amount reaching me? In any case, I do not think we need to compromise here on
  privacy."
  He was right on both counts, and the answer was already in the product. **The football field works
  because the server sends MULTIPLES and the browser multiplies them by a figure that never leaves
  it.** The investor list now works the same way: `reveal_payload` sends the candidate houses WITH
  their published cheque ranges, which are our data about funds rather than the founder's data about
  themselves, and `invChequeFits` in the browser drops the ones that cannot fund the round. Six extra
  candidates go out (`CHEQUE_FILTER_OVERFETCH`) so the list cannot come up short, and the over-fetch
  reveals nothing: every house sent already qualified on sector and stage, and which ones the browser
  hides is decided on the far side of the wall and never sent back.
  **MEASURED ON ALL 102 FIXTURES AT A $500K RAISE: the browser path gives the IDENTICAL list to
  sending us the raise, 102 of 102.** Nothing is lost. The raise is not added to the allowlist and
  the ruling is closed with no compromise.
  **The cost is one rule in two languages**, which is the shape that drifts, so check 15 now runs
  both copies over every distinct cheque shape in `data/investors.csv` at five raises and fails on
  the first disagreement. 345 cases today; breaking the page's copy on purpose fails it with the
  house, the raise and both answers named. The note under the list is rewritten from what is
  actually on screen, because the server's count is of what it sent.
  Suite: nineteen checks, all green; golden 0 of 102.

- **7 Sep, 14:50 UK (Fable)**. **Landing page 3 rebuilt as "Under the hood", from the design canvas
  (five rounds with Daniil, 6 to 7 Sep; brief at `claude/Fairway_page3_brief_6Sep.md`).** The page
  sells the selection machinery, not the database: four rules on top (business model not sector code;
  every name has to earn its place; your figures stay in your browser; a banker's view if you want
  one), a rail of four numbered steps, and four cards on one bottom edge: two funnels (listed and
  private rounds, each ending at the set that prices the range) with the six listed peers and the
  nine rounds under them; how Fairway helps position the company; the questions to expect; and a
  ranked list of eight houses from `data/investors.csv` (the three whose recent deals sit in customer
  engagement first). Call to action top right, large. The old block "the houses behind your
  reference rounds" and the blurred card are gone. Everything sits in one screen at 1440 x 900 and
  stacks below 1100px. **Rulings applied on the way:** no counts in headlines; no lines that read as
  AI; the example company never named; the review happens only once the founder shares the numbers and the field, so
  page 2's step 4 keeps "Has a banker check every line" with that condition written in, and the
  hero's third stat reads "A banker reviews it, once you share it". **Numbers aligned across the
  pages (Daniil, 15:20 UK: "super important"):** hero "5 to 7 peers"; page 2 revenue row "6 core
  peers" with the band unchanged and "+6 more" gone from the row and from "Our read"; page 3 six.
  The gross-profit row on page 2 could not be aligned without inventing figures; see the box. **Check 18**
  (`tools/check_landing_page3.py`): `--write` puts the four pool figures (525 listed, 320 rounds, 27
  and 25 in the marketing-software bucket) into `landing-counts.js` and the HTML from the loader, and
  the check fails the suite if the page and the data ever disagree, or if an investor row's cheque,
  currency conversion or deal is not in the table. Tested by breaking it. World figures carry their
  source on the page: 53,000+ listed companies (World Bank, 2025), 24,000+ companies raised venture
  money in 2025 (Crunchbase, 7 Jan 2026). Suite: nineteen checks, all green; gate 97; golden 0 moved.
  **Two things found, in the box above:** the example's multiples are not the engine's; page 2 says
  12 peers. Not done: page 2's "12 peers"; the reason lines on the six were shortened to fit one
  line each (copy, not figures). Files: `index.html`, `landing-p3.css`, `landing-p3.js`,
  `landing-counts.js`, `tools/check_landing_page3.py`, `tools/check_all.sh`, this document.

- **7 Sep, 17:40 UK (Opus)**. **Daniil's nine answers of the afternoon, applied.** Suite green
  throughout; golden rebaselined with every move attributed (84 fixtures gained the new
  trailing-versus-forward flag and nothing else, 16 lost a per-customer reading).
  **THE BIG ONE, AND IT WAS HIS CATCH.** "We decided to exclude the super high user-based multiples,
  given they are calculated for B2B clients and such businesses do not price on that basis." He was
  right and the data is emphatic: as one bucket, enterprise value per business customer ran $3,200
  to $760,000,000, a spread of 237,500 times. It is the Fireblocks ruling of 6 September written as
  a rule rather than a name: a business customer is not one kind of thing either. Doctolib has
  300,000 practitioners at $19,333 each; Virta Health has 100 employer contracts at $20,000,000
  each. **`PER_CUSTOMER_BUSINESS_MAX = $1m`** removes 20 of the 131 per-unit readings, each named on
  every run. **The widest per-user chart falls from 1,034x to 97x**, against a median revenue chart
  of 3.3x. Nothing else about those 20 rounds changes: they keep their revenue multiple, their names
  and their sources.
  **AND IT CLOSES THE WIDTH RULING WITHOUT A NUMBER.** The open question was what spread should hide
  a per-user chart. At 97x there is nothing left to hide. The lever stays in the code, unset, with
  the measurement beside it.
  **EVERY FIXTURE NOW CARRIES A STAGE** (his ruling). Seed, as a stated test condition, not a claim
  about any of the 102: it is the modal answer for a company taken off Y Combinator or Product Hunt,
  and the aggregate barely moves on the choice (813 cards at Seed, 724 Pre-seed, 797 Series A). The
  live investor path is now exercised by the test set for the first time since 6 September.
  **THE TRAILING-VERSUS-FORWARD WARNING IS WRITTEN** (his ruling): when a founder gives no growth
  rate their trailing figure stands in for the next twelve months, on both readings, and the caveat
  now says which direction the error runs rather than leaving them to guess. Severity BASIS, so it
  is the first thing they read.
  **THE LEVERAGE FLAG CONFIRMED AS A SENTENCE, NOT A FENCE.** His words: it "should not be a barrier
  for showing that in the comps set if the comp is relevant from business point of view." It never
  was one: the name shows, prices and sits in the median exactly as before, and the founder gets one
  sentence saying how much of that company's enterprise value is debt.
  **THE STALE RULE EXPLAINED AND LEFT PER FILE.** His instruction is that the latest numbers win.
  That is what per-file does; the global version did the opposite, taking 511 rows at 1 September out
  of service because 14 rows arrived dated 5 September. **One thing for him:** the 14 new names carry
  1-September market data four days newer than the other 511, so the next Capital IQ refresh should
  include them and put the whole universe back on one date.

- **7 Sep, 15:10 UK (Opus)**. **The public pull loaded, the profiler built, the payload served, the
  field wired. Eighteen checks and all of them pass, which has never happened before.** Worked in a
  fresh clone of origin at `2cc90fa`; no git ran on the laptop; the files were written into the
  working tree over the bridge.
  **THE LOAD.** `tools/load_public_pull_16_7sep.py`, 16 in, 14 out, 2 named, not one figure retyped.
  `data/peers-mixed.csv` is a sixth peers file loaded as SECONDARY so no existing archetype is
  re-counted. Check 1 green for the first time since 5 September. Gate holds at 97 with the same five
  failures. Golden moved on 100 fixtures: 21 gained one of the new names, 15 moved on the regenerated
  token weights, 64 only gained a new key on the range object.
  **THE LEVERAGE FLAG** the verdicts asked for and the repo did not have: computed on every row,
  `LEVERAGE_FLAG_PCT = 80` catching Claritev (87.5%) and Skillsoft (89.0%) exactly. It reaches the
  founder as a sentence and moves no number.
  **THE PROFILER**, `selector/profiler.py`, which `docs/engine-architecture.md` has specified since
  24 August and nothing built. It is what `payload: null` was really waiting for. Vocabulary computed
  from the tag files; anything outside it dropped and recorded; product tags fenced by shape; no
  number from the model, ever. **Check 17** drives it with a model misbehaving on purpose, including
  a website telling it to output a valuation and print the key.
  **`api/payload.py`**, a Python function beside the Node one, so the engine is not ported to
  JavaScript. Tier is the server's; a browser claiming paid gets free. Check 15 extended over it:
  same 15 names as the page, all 36 figure and free-text fields refused off a hostile body.
  **`field.js` rows 4, 5 and 6** price on the engine's lanes instead of printing "in build", which
  closes the last second source of truth on the reveal.
  **The third line in the quiz**, editable, with the working under it, and it never leaves the
  browser.
  **`tools/render_harness.py`**, 102 fixtures on one contact sheet.
  **FOUR THINGS FOUND, three older than today.** (1) The stale-universe rule was GLOBAL: one file
  arriving on a newer date marked all 511 existing rows stale and stripped every multiple, and the
  gate still read 97 because it counts names rather than prices. Now per source file, same 13 rows
  stale as before. (2) Check 3 had been red since 6 September on `n_estimates` and
  `revenue_growth_cy3_pct`; the suite has had two red checks, not one. (3) **A third door on the free
  tier**: the honesty caveats print figures and are written about the private lead range, so a free
  founder read "At most 12.2x" under the blur. The harness found it on its first run. The caveat is
  now said without the figure, and check 14 reads the prose. (4) Check 14 passed `raise_musd=3.0`,
  which rule E9 makes impossible; with no raise it is 0 callable houses on all 102, because the
  fixtures carry no stage. The live path is fine; the test was not.
  Suite: gate 97, golden 0 of 102, eighteen checks, none red.

- **6 Sep, 23:35 UK (Fable)**. **Daniil's fifteen answers on the decisions box worked through.** Done:
  investor ranking puts stage specialists first inside a tier (`selector/investors.py`); Accel split
  into Accel (evidence) and Accel Atoms (callable, no deal yet so not rendered)
  (`tools/apply_accel_split_6sep.py`, 532 in, 533 out); six stale `deal_note` tails removed and Y
  Combinator's band corrected to All stages (`tools/apply_investor_fixes_6sep.py`, 532 in, 532 out,
  7 edits); Factorial's basis aligned to its own note (net); ultrasonium's end market set to
  Manufacturing with its four customer markets as product words (gate 96 to 97); every pound figure
  on the landing converted at 1.35 $/£ with a rate note, Episode 1 at the website's range. 86
  fixtures rebaselined: investor lists reorder for most founders, peers move for ultrasonium and the
  lanes holding Factorial. Restated in plain words for ruling: the per-user chart width, the
  next-twelve-months figure when growth is unknown, the raise amount on the free path, check 15
  without node. Closed with a reason: Edenred and Pluxee, Kriya and Vegrow and Fireblocks, unifold
  (to the bulk pass). Suite: gate 97, golden 0, seventeen checks, only check 1 red.

- **6 Sep, 22:50 UK (Fable; stamped off the clock, and placed above the 23:45 entry, whose stamp
  was ahead of it)**. **The status document cleaned up on Daniil's instruction**, so that nothing
  decided still reads as open: the rulings list is down to three (per-user chart ceiling,
  ultrasonium's end market, Factorial's basis); the Fable to-do is marked done with a pointer to the
  write-up; the taxonomy rule is recorded as ruled yes, build after the pilot; the decisions box no
  longer lists things already closed. **Three re-tags applied** (`tools/apply_retags_6sep.py`,
  170 in 170 out and 204 in 204 out): Wix to Consumer & Prosumer Software | Business Applications,
  function Productivity, on "a website builder, just software" (Wix leaves bluerails' core and the
  cloud lanes of context-dev and maritime, joins acti's core and the wider rings of chronicle and
  sim); Owner's Marketing secondary dropped (leaves elentaria, pazi, akkari); AuditBoard to
  Business Applications first (0 move). 12 fixtures rebaselined on those reasons. Suite: gate 96,
  golden 0, seventeen checks, only check 1 red.

- **6 Sep, 23:45 UK (Fable)**. **The push did not happen** (`git add -A` then `git push`, no commit
  between; origin still `5e518d2`); the late batch joins the staged one. **The ceiling ruling applied
  throughout:** of 84 held rows, 12 carry a bounded multiple, 10 held for the bound alone are
  released (`tools/apply_ceiling_ruling_6sep.py`, 268 in, 268 out, 10 changed: Anthropic x2,
  Databricks, Docker, Miro, Notion, PayFit, Vercel, Decagon, Factorial), 2 stay held with the reason
  named (Perplexity Jan-24: valuation press-reported; Marqeta May-20: basis unstated); 27 fixtures'
  private ranges gain those names, golden rebaselined on the ruling. **The "also compare with" list
  built** (`also_compare` on the profile; a named row passes the family gate and the relative floor
  for that founder only, is marked pinned with the reason, and check 8 prints every pin): Vention
  and Applied Intuition for manifold-robotics, Whatnot and StockX for tash; exactly two fixtures
  change; **gate 94 to 96**. **BVNK** to Crypto & Digital Assets | Cross-Border & FX (trolley and
  dots gain it). **The "293" explained and check 8 now prints the split**: 294 of 1,117 comparable
  slots on weak evidence (217 on a generic word, 77 on the end market alone), 116 of 204 lanes touch
  one, 3 lanes rest on nothing else. **One flake fixed**: word points summed in sorted order, golden
  stable across hash seeds. Suite: gate 96, golden 0, seventeen checks, only check 1 red.

- **6 Sep, 22:20 UK (Fable)**. **Daniil's thirteen afternoon follow-ups worked; four things applied,
  eight matcher changes measured and rejected, the rest marked TAKEOVER below.** Fresh clone of
  `5e518d2`. Applied: StockX's gross ceiling released into the medians on the threshold ruling
  (`tools/apply_stockx_release_6sep.py`, 52 in, 52 out, 1 changed); BVNK swapped to Crypto & Digital
  Assets first (`tools/apply_bvnk_swap_6sep.py`, 204 in, 204 out, 1 changed; golden text-only move
  on agentcard and unifold, rebaselined); **check 16** (`tools/check_token_weights.py`) recomputes the
  word-weight file from the seven tag files on every run and fails if it differs, so the two-week
  staleness cannot recur; **kind 3 on the No-comps list** in check 8: 293 comparables across 69
  fixtures admitted on generic words ("marketplace", "automation") or the end market alone,
  recorded not removed. Measured and rejected, each with the names it let through: five family-gate
  bridges, a per-tier relative floor, a stricter end-market route, and re-tags of manifold and tash
  (full table in `docs/matching-refinement-6sep.md`, "Follow-ups"). The family gate is right; the
  names Daniil wants for manifold and tash need a named allow-list, not a looser rule. Answered in
  the write-up: the three fixtures ARE on the No-comps list (kind 2); Accolade is the take-private
  value; the six payments rows differ in the fields the engine reads; Mensch und Maschine and
  DocuSign never reached ultrasonium (a rejected variant only); Owner is out of fundraisly since the
  weights were regenerated. Suite after: gate 94 of 102, golden 0, seventeen checks, only check 1
  red.

- **6 Sep, 12:20 UK (Fable; placed first by arrival, the 13:00 stamp below is ahead of the clock).**
  **Matching refinement measured, nothing in the engine or data changed.** Fresh clone of origin at
  `fb260b2`; suite run before and after on that clone, byte-identical: gate 95 of 102, golden 0 of
  102, only check 1 red (fifteen checks on origin; the merged working tree has sixteen and was not
  measured). Full write-up `docs/matching-refinement-6sep.md` (project:
  `claude/Fairway_matching_refinement_6Sep.md`); the stage-by-stage graphic
  `docs/matching-stages-6sep.html`, published as an artifact. (1) **levelten, manifold-robotics and
  tash are sourcing gaps, not matcher failures.** Every held round in each archetype is listed with
  the fence that stops it (36, 21, 36 rounds). levelten's one true comparable, Xpansiv, has no
  revenue multiple; tash's two true comparables, Whatnot and StockX, carry GMV only and a held-out
  gross ceiling; manifold's second name, Vention, sits behind the family gate, the relative floor
  (7.2 against a bar of 11.9 set by Berkshire Grey's 26.5) and a doubtful gross label. Six matcher
  changes were measured on a scratch engine and every one that moved these fixtures let a wrong
  name through elsewhere (Carta into tash, Vention into sellerclaw), so none is proposed; all three
  stay on the No-comps list. The 24-month window is not a fence any more and is removed from the
  list of reasons. (2) **The taxonomy rule measured:** 29 of 102 change peers, 21 gain, 29 lose,
  gate 95 to 94 (alloovium, which was passing on label-only names). It removes TomTom from seven
  AI-tooling fixtures and five other-industry vertical rows from edviro. Same result whether or not
  the fallback obeys it. (3) **The big finding: `data/tag-token-weights.csv` is stale.** Last
  computed 24 Aug from five tag files; seven exist and the private set has more than tripled. 400
  words are generic by its own rule, it lists 207, and 193 generic words ("own", "health",
  "warehouse", "freight", "deposit") score at full weight. **246 of the 1,185 core and private names
  shown today are admitted on generic words alone**: inato, a clinical-trials marketplace, is shown
  eBay, Etsy, Whatnot and Vinted on "marketplace". Regenerating (the existing rule on current data,
  `tools/regenerate_token_weights_6sep.py`) changes 75 of 102 fixtures, gate 95 to 94,
  human-verified peers surfaced 15 to 16 of 29; proposed, not applied, because of its size.
  Tightening the relevance gate was measured at three settings and rejected (gate 93, 91, 81).
  Re-tags proposed with their measured effect: ultrasonium's industry to Manufacturing (listed core
  becomes PTC, Dassault, Lectra, Xometry; the archetype change alone is a trap that passes the gate
  on DocuSign via the word "contract"); Owner loses its Marketing secondary (leaves six
  non-restaurant lanes); AuditBoard to Business Applications (0 move); apollo-atomics keeps its
  tag; Vention's gross label to the B3 audit. Three tools added; the six files were written into
  the working tree over the bridge, no git run. Commands for Daniil at the foot of the write-up.

- **6 Sep, 12:30 UK (Opus, second session)**. **The no-storage reveal, built and checked.** Daniil:
  "we only take a record of the company data (profile, website), without storing the numbers... if
  the user wants to have his numbers and ff reviewed, he presses a button and it comes through to
  us." Full write-up in `docs/handover-2026-09-06-opus-privacy.md`.
  **THE BRIEF NAMED ONE DOOR AND THERE WERE TWO.** `submitLead()` posted the whole `responses`
  object plus every figure `computeResult` had worked out to `/api/lead`, which writes them into
  named columns in the Google Sheet and logs the record, **at the email step, before the founder
  sees anything**. A promise covering only `/api/reveal` would have been false the moment anyone
  typed an email. Both doors are closed, and both at both ends: the page does not send, and the
  server refuses what arrives without consent.
  **The request body is 15 fields and every one is a label or a percentage:** the profile (stage,
  sector chips, sector in their own words, website, company, country, how they charge, and the
  three fork labels) plus growth in its three shapes and gross margin. Removed by name: `revenue`,
  `revenue_exact`, `arr_exact`, `recurring_pct`, `profit`, `raise`, `timing`, `currency`,
  `growth_detail`, `concern_notes`, `context_link`, `ebitda_ltm`, both `last_round` figures and the
  three computed metrics. Three of those are free text, which is where a founder writes a figure
  when no box asks for one.
  **Why it is nearly free, and the repo already proved it:** all 102 fixtures carry no revenue, no
  growth and no margin, and 95 pass the gate. **One thing the brief missed and it mattered:**
  `investors._stage_for` derived the founder's stage from the size of their raise, and the stage
  band is a hard gate. Against this merged tree, with every one of the five houses now carrying a
  published band, taking the raise away and not reading the stage gives **0 callable cards and 102
  of 102 fixtures empty**. That was a bug, not a price: **the founder answers "what stage are you
  at" in step 1 and the engine never read it.** `_stage_for` reads the stated stage first: 813 cards
  at Seed, 724 at Pre-seed, 797 at Series A, no fixture empty, which is the same 813 the raise gave.
  What is actually lost is `_cheque_fits`, which excluded **3 of 156** callable houses at a $3m
  raise; those three now show with their published cheque range on the card.
  **Built:** `reveal-request.js` (the only place in the product that builds a request body; three
  builders, each a loop over the allowlist, none containing a field name of its own);
  `reveal-figures.js` (the figures in one variable in one closure, no browser storage of any kind,
  and the multiplication); `reveal-client.js` rewritten to read the payload, so the peer charts, the
  honesty caveats, the fix list and both investor layers render from **one selection run** and
  `renderRecommendations` and `renderInvestors` are called for the first time; the consent button
  beside the 24-hour promise, naming the founder's own answers one by one and storing the wording
  clicked; the plain sentence before the quiz, on the hero and again on the quiz screen; both data
  disclaimers rewritten, because the old one said the commentary service "receives the business
  figures alone" and it now receives none. Rulebook **E9**.
  **Two things the wiring found:** chart entries carry no peer names (they live in `ranges`), so
  each bar is joined back for them; and the engine has always said `RANGE`, `SCATTER` or `DIAMOND`
  and nothing read it, so a one-name reading is now drawn as a point labelled "one name is not a
  range" (A7) rather than as a bar of no width.
  **Check 15**, `tools/check_request_boundary.py`, half reader and half runner. It gives every
  figure a **sentinel value that appears nowhere else in the repo** and looks for those values in
  every request body, in the Vercel log line and in the prompt that goes to the model, with the
  outbound model call intercepted; it handles `api/lead.js` three times and asserts 14 figure fields
  are refused without consent and stored with it; and it compares **1,176 multiplications** of the
  browser's arithmetic against `round(v * low, 2)` over the real charts of all 102 fixtures. Tested
  by breaking it: adding `revenue_exact` to the allowlist fails it nine ways.
  **Suite on the merged tree:** golden 0 of 102 moved, gate 95 of 102, only check 1 red. Also run in
  a real browser: no page errors, the actual POST carries no amount, the free payload renders the
  private lanes blurred with names and no figures anywhere in the DOM.
  **Not done and named:** the engine payload is not served yet (`payload: null` with a reason; the
  Python side is the next piece and the boundary does not change when it lands); the page can price
  one basis today, because the quiz asks for monthly revenue and the engine's REVENUE basis is
  trailing; `field.js` still draws its own nine method rows.

- **6 Sep, 12:30 UK (Opus, second session): TWO THINGS FOUND IN THE 13:00 INVESTOR-STAGE WORK.**
  Found while comparing the working tree against origin file by file, without running git.
  **(1) Golden moved and the log entry does not say so.** Six fixtures changed: acti, befreed,
  bloomy, honen, welltory, wondering. The reason is sound and is the intended consequence: Haystack
  now carries a band, so it enters those six founders' call lists at tier 1. It still needs writing
  down, because "golden must not move without the reason written down" is the rule that catches the
  ones that are not intended.
  **(2) Five `deal_note` fields still say STAGE BAND LEFT EMPTY, and `investors.js` prints
  `deal_note` on the founder's card.** Canaan, Founderful, Haystack, Techstars and Town Hall
  Ventures each carry a band now and a note beside it saying the band was left empty. That is a
  false sentence on a card a founder reads. Not fixed here, because it is that session's work in
  flight and the wording is a ruling; flagged so it does not reach the pilot.

- **6 Sep, 13:00 UK (Opus)**. **"Early stage" is a stage band.** Daniil: "some of these houses
  clearly state that they are early stage. It is good enough, we dont have to look for them to say
  exactly Series A or Seed." Applied: Founderful and Haystack carry **Early stage**, Town Hall
  Ventures carries **All stages** ("We invest across all stages"), and Techstars becomes
  **Pre-seed; Early stage**, which is what its own page says. Each band is the firm's published
  phrase, with the wording printed beside it in the script.
  **The half that would have gone wrong silently:** the stage band is an EXACT STRING match and a
  HARD GATE (`if stages and stage and not stage_hit: continue`), so writing "Early stage" on its own
  would have EXCLUDED Haystack from every seed founder rather than including it. `STAGE_COVERS` in
  `selector/investors.py` is the other half: the two phrases expand to Pre-seed, Seed and Series A,
  which is the whole of what a founder can be, because those are the only three buttons in the quiz.
  The cheque range does the rest of the filtering, which is the right instrument for it: Town Hall
  says all stages and writes $3m to $30m initial cheques, so `_cheque_fits` keeps it away from a
  pre-seed founder raising $500k. 532 rows in, 532 out, 4 changed, 0 dropped.
  **Refusals 18 to 5 to 3 to 0. The check and the engine now agree exactly, both at 156 of 156**,
  which closes the contradiction that opened this item. 813 callable cards, 8.0 a founder, and no
  fixture gets fewer than three houses.

- **6 Sep, 12:15 UK (Opus)**. **Investor stage pull checked and applied.** Daniil supplied stage
  bands for the five houses `investor_check.py` refused. All five pages fetched and the quoted
  wording verified word for word; the transcription was accurate on all five. **But it is not five
  stage bands.** Canaan publishes one ("seeking seed and Series A funding") and Techstars publishes
  one ("a leading pre-seed and early-stage venture capital firm"; the Accelerator Funds "invest on
  standardized terms in pre-seed companies"). The other three name no stage at all: Founderful says
  "lead investor in their first financing round", Haystack "the earliest stages", and Town Hall says
  outright "we invest across all stages", which is a positive claim of no restriction rather than an
  omission. Applied by `tools/apply_investor_stages_6sep.py`: 532 rows in, 532 out, 2 changed, 0
  dropped, and it refuses to overwrite a band that already exists. **Refusals 18 to 5 to 3.** Two
  things the pull confirmed without changing: Haystack's "$1M to $3M in initial rounds" and Town
  Hall's "$3M to $30M" initial cheques are already in the file at exactly those figures, which is
  independent verification of two rows we held. One discrepancy flagged not resolved: haystack.vc's
  body says $1M to $3M and its own meta description says $500K to $2M. Raw file kept as
  `data/raw/2026-09-06_investor-stage-gap-5houses.csv`.
  **The prompt for the privacy architecture is written**, `docs/prompts/privacy-architecture-6sep.md`,
  for a separate Opus session. It carries the measured selection-versus-pricing boundary: growth and
  gross margin must reach the server, every absolute figure can stay in the browser, and revenue
  touches selection in exactly one degradable tie-break.

- **6 Sep, 11:30 UK (Opus)**. **The listed growth data was stale, and that is the real answer to
  the CY+0 versus CY+1 argument.** Daniil: "ALL peers in the dataset I provided measure CY+1 to
  CY+3 CAGR... align all data to that." He was right. The five peers files carried growth in three
  columns and NONE of them matched the database he supplied on 1 September, which had been sitting
  in `data/raw/2026-09-01_listed-full-refresh.csv` with 500 rows and a single `cagr_cy1_cy3_pct`
  column since the day it arrived. Ours were rounded to whole percentages (Visa 12.0, Alibaba 18.0,
  FedEx 4.0), his carry a decimal (10.9, 12.8, 1.8), and 5 of 166 software rows agreed. The 1-Sep
  refresh had updated the market data and never touched growth. **I was wrong twice before getting
  here**, first calling the two CAGRs one measure and then calling them a year apart; both were
  arguments about column names when the numbers themselves were old.
  `tools/align_growth_to_1sep_6sep.py`: **533 rows in, 533 out, 516 matched and updated, 0 dropped**,
  the 17 unmatched named in the output and left exactly as they were. His three year-on-year rates
  and his broker-estimate count came across too, the latter being the figure the engine has wanted
  since August to turn the micro-cap exclusion list into a rule. The stale `cagr_cy0_cy2` column is
  blanked on every row it filled. **495 of 511 rows now read CAGR_CY1_CY3 and 498 can rank a peer,
  against 319 before**, because the 123 logistics and services rows had no usable horizon at all.
  Gate holds at 95, golden 0 of 102 moved.

- **6 Sep, 10:40 UK (Opus)**. **A customer is not one kind of thing.** Daniil asked what the
  20,588x per-user comparable was: unifold's chart, holding Fireblocks at $10,000,000 of enterprise
  value per customer beside MoonPay at $486. Fireblocks had about 800 institutions, MoonPay about
  7 million retail consumers. Two measures sharing a label, exactly like gross and net revenue, and
  across all 89 rounds tagged CUSTOMERS the two run from $360 to $760,000,000. **The data already
  knew which was which**: every row carries a `buyer` field and BUSINESS_CUSTOMERS was already a
  basis with zero rows using it. A round whose buyer is CONSUMER now counts consumers and every
  other round counts businesses. **Median spread on the per-user charts 33x to 12.6x**, in line with
  the revenue charts at 3.4x. Nothing excluded, no threshold invented. Then his second ruling, which
  goes further: **Fireblocks carries no per-customer reading at all** ("a B2B business, they do not
  price on number of customers"), applied by `tools/kill_fireblocks_customer_6sep.py`, 268 rows in,
  268 out, 2 changed, 0 dropped, revenue multiple and every source URL untouched. unifold's chart is
  now $233 to $378 per registered user.

- **6 Sep, 09:45 UK (Opus)**. Daniil's five rulings of the morning applied and the suite re-run:
  gate 95 of 102, golden 0 of 102 moved, only check 1 red. (1) **A founder who gives no numbers still
  gets a reveal**: the per-user chart now opens where the founder's own picked rounds can price it,
  two rounds minimum, so it reaches 54 fixtures instead of 6, and 63 fixtures can show three or four
  charts instead of 33. The payload carries a `charts` list in his reading order. (2) **The free
  tier**: public complete and free, everything else blurred with the comparable names and dates on
  hover and no figures anywhere in the payload; rule E8 amended and check 14 now walks all 102 free
  payloads to enforce it. Found on the way: the private lane had never carried a hover table at all.
  (3) **The growth definition settled and tested**, 16 of 16 rows; the public pull is unblocked and
  the wrong code comment corrected. (4) **`investor_check` brought into line with the 3-Sep cheque
  ruling**, refusals 18 to 5. (5) **Udemy released**, and reading the 8-K shows the figure we held
  was already his method. Also: `basis_compatible()` deleted. Handover
  `docs/handover-2026-09-06-opus.md`.

- **6 Sep, 00:20 UK (Opus)**. Daniil's two answers applied. (1) **The gross range is revealed when
  the founder gives the gross number, the net range when they give the net number, both when they
  give both.** That is the product rule and the engine already does exactly that; nothing changed.
  The open question was only about check 8, whose 102 test companies give no figures at all, and it
  is settled: the scoreboard counts comparables found rather than prices shown, so the gate stands
  at 95. (2) **`basis_compatible()` deleted.** Never called by anything, described as the fence in
  four documents, and the real fence is `basis_mult()`. Its docstring now says it is the only fence
  and that changing it is changing rule B3; the lender guard and the unknown-reads-as-net rule are
  written there. The two 31-Aug documents that describe the deleted function carry a dated
  correction rather than a rewrite, and the live instruction in
  `docs/brief-for-fable-basis-and-period.md` now points at `basis_mult` and at the listed-lane hole.
  Suite re-run after the deletion: golden 0 of 102 moved, gate 95, only check 1 red.

- **5 Sep, 23:55 UK (Opus)**. All six Opus to-dos done, in a fresh clone of origin at `6433348`; no
  git ran on the laptop. **Net and gross both, built:** eight forks ask for the second figure, the
  engine carries a `REVENUE_GROSS` reading against rounds priced on gross, rule B3a written, six new
  assertions in check 9 hold B3 in place, **gate 90 to 95** with clera, nursa, paymentkit, standout
  and tsenta flipping and nothing regressing. **The No-comps list** named in check 8 with both kinds
  of entry; A12 parts 2 and 3 and A11 rewritten. **The thin-lane diagnosis** fixed: 3 sourcing
  requests, 8 awaiting a ruling, 1 matcher question, instead of "0 genuine". **The public-pull dates**
  corrected to 5 Sep in both files, names deliberately unchanged. **The fix list** mounted in
  `index.html`. **Engine to reveal started:** `selector/reveal_payload.py` plus check 14, 102 of 102
  fixtures assemble, and the honesty strings reach a payload for the first time. Golden rebaselined,
  93 fixtures, reason written down and nothing outside `all_ranges` moved. Three things sent up:
  the gate-gating ruling, `basis_compatible()` being dead code, and the held rows already released.
  Handover `docs/handover-2026-09-06-opus.md`.

- **5 Sep, 23:40 UK (Fable)**. Fourth ruling of the evening (23:35 UK): the quiz asks a founder for
  both net and gross revenue, with the difference explained beside the question, and the engine
  stores and uses both, each against rounds on its own basis. Written in as the first Opus to-do and
  as a given in the Fable matching prompt. Closes the reason four of the 5-Sep rounds price nobody.

- **5 Sep, 23:30 UK (Fable)**. Daniil's three rulings of 23:10 UK written in: (1) the word
  "register" is out, the list is the **No-comps list** and holds both the companies served on a
  label and the companies with no peers at all; (2) the four dates (8, 11, 15, 18 Sep) are marches of
  30 to 40 new test companies, not data pulls; the pool is large enough, the pilot finds the blind
  spots, the daily two to three hours of data work stop, and the No-comps list is resolved in bulk
  once before launch; (3) the work in between is on the matching machine and the tagging, a separate
  Fable to-do with its prompt, now in TO DO (i).

- **5 Sep, 23:22 UK (Daniil, `6433348`)**. Second load of the evening: the floor-over-floor rows
  released (Moss 14.29x, Restaurant365 10.0x, AuditBoard 15.0x back in the medians; Udemy still
  held), three control anchors, investor table 506 to 532.

- **5 Sep, 22:55 UK (Fable)**. Audit of the 5-Sep log done against a fresh clone of origin at
  `d1f8966`, suite re-run: gate 90 of 102, golden 0 of 102, 142 callable houses, 813 cards, check 1
  red on the public pull alone. Eight findings sent to Daniil at 22:42 UK, including that the
  thin-lane tool's "0 genuine" headline contradicts the pull that moved eight fixtures, that three
  log entries dated 4 Sep are 5 Sep, and that A12's wording is looser than the code. **All three of
  those are fixed in the 23:55 entry above.**

- **5 Sep, 22:15 UK (Opus; the entry itself was stamped 22:30, ahead of the clock)**. Relevance gate
  ruled and built; pushed 22:34 UK as `d1f8966`. The archetype fallback is a rescue that opens only
  for a lane that cannot price, fires for 2 of 102 fixtures, and names every comparable it admits in
  check 8. Gate 89 to 90 of 102. Rulebook A12. Taxonomy measured in `docs/taxonomy-review-5sep.md`.

- **5 Sep, 21:55 UK (Opus)**. **Float adjustment RULED OUT by Daniil: his figures stand**, because
  the earnings multiples are aligned (Edenred 12.5x, Pluxee 10.0x) and P/E is untouched by the
  bridge. Pushed as `efe2321` (21:54 UK). **The 5-Sep pull loaded** as `dad1b3f` (21:54 UK): 27
  rounds in, 27 loaded, zero duplicates; 24 tag rows; investors 445 houses to 506, callable 92 to
  142; gate 81 to 89 of 102, nothing regressing.

- **5 Sep, 21:35 UK (Opus)**. Float adjustment corrected (pushed `a654e1e`, 21:50 UK), then
  superseded twenty minutes later by the ruling above.

- **5 Sep, 21:27 UK (Opus; the entry was dated 4 Sep, corrected in the 22:55 audit)**. Public pull of
  16 names received as a screenshot and TRANSCRIBED to `data/raw/2026-09-04_public-pull-16-names.csv`;
  there is no CSV and there will not be one, so the transcription is the record (D1). Arithmetic
  verified on all 16. Seven issues written up. **The file's own header and the verdicts document were
  corrected to 5 September on 5 Sep at 23:55.**

- **5 Sep, 21:15 UK (Claude)**. Handover `docs/handover-2026-09-05-claude.md` written: 27 rounds,
  24 tags, 19 houses, every rejection named.

- **5 Sep, 20:26 UK (Opus; the entry was dated 4 Sep, corrected in the 22:55 audit)**. Both pull
  documents rewritten on the new division of labour. Two errors caught in the process. **Eighteen of
  41 proposed tickers were already in the file**, including Wix and JFrog. **Four more no longer
  trade** (Sapiens, LTG, Udemy, Accolade), now rulebook **C7**. Also found: the basis trap. Four
  fixtures had full private lanes that priced nothing because every round in them is GROSS and the
  fixture defaulted to NET. 71 of 290 private rounds were gross at that count; it is 78 of 320 now.
  **That trap is what the 23:35 ruling and the 23:55 build closed.**

- **5 Sep, 20:05 UK by the clock (Fable; the entry was dated 4 Sep, corrected in the 22:55 audit)**.
  Landing v9.1, round 2 of Daniil's markup: the three Our read columns now end on one line. Universe
  box shows Appian, HiBob and Factorial. Likely investors gains Seedcamp and Connect Ventures, both
  from `data/investors.csv` with a source URL. Note 6 now leads with the investors and the fixes.

- **4 Sep, 20:19 UK (Opus)**. CAB Payments repriced on the same ruling as XP. The gate re-scored on
  the new pass rule: 81 of 102. Pricing moved to three tiers and written up, with rulebook **E8** on
  the lock being a payload boundary. `regression_range()` is built, gated at R2 >= 0.50 with an
  extrapolation limit, and it needs the founder's own growth rate, which no fixture carries. Pushed
  as `e35e96e` (20:23 UK).

- **4 Sep, 20:10 UK (Opus)**. **D10 broken by me, and the fix is in the suite.** Check 0 runs
  `git status` and `git fetch`, and I ran the whole suite through the bridge, which left exactly the
  `.git/index.lock` D10 exists to prevent. `check_all.sh` and `check_pushed.sh` now both honour
  `FAIRWAY_NO_GIT=1`.

- **4 Sep, 20:02 UK (Opus)**. Batch pushed as `195ebf7`. Four rulings applied and documented in
  `docs/rulings-applied-4sep.md`. New rule **D14**: a ruling, a rule change or a data pull is
  committed the moment it lands. Pushed as `74f0c7d` (20:13 UK).

- **4 Sep, 19:50 UK by the clock (Fable).** Landing v9.1: Daniil's page-2 markup implemented. An
  empty branch `landing-v9.1-page2-markup-4sep` was created on origin and not used; delete it or
  ignore it.

- **4 Sep, 19:45 UK (Opus)**. Recommendations Day 3 built and checked (five dimensions live, check
  13 added, renderer written). Check 0 added, `tools/check_pushed.sh`.

- **4 Sep, 19:35 UK (Fable)**. This document created; tracker and merged roadmap archived.

## Where the old documents live

`claude/Fairway_tracker.md` (the rolling tracker, 27 Aug to 4 Sep, including the full append-only
log, the durability protocol and standing rules, still binding, preserved there) and
`claude/Fairway_roadmap_MERGED_3Sep.md` (the merged roadmap) are archived in the project with
pointer headers. Repo copy of this document: `docs/STATUS-2026-09.md`. Today's handovers:
`docs/handover-2026-09-05-claude.md` (the pull), `docs/handover-2026-09-05-opus-to-fable.md` (A12 and
the taxonomy) and `docs/handover-2026-09-06-opus.md` (net and gross, the No-comps list, the reveal
payload). Strategy documents, project only, no repo copy: `claude/Fairway_strategy_review_7Sep.md`
(the three ideas, the three horizons, who to talk to, every source) and
`claude/Fairway_competitor_Equidam_7Sep.md` (the competitor read).

---

## TAKEOVER: what Fable did not do on 6 September, so the next session can pick it up

Each item is scoped, has its measurement or its reason, and names the file it lives in.

1. ~~The "also compare with" list~~ **BUILT 6 Sep, 23:45 UK** as `also_compare` on the founder profile,
   printed by check 8 under PINNED BY NAME. What is left: it lives on the test-company profiles in
   `selector/golden_profiles.py`; the live product needs the same field on the founder profile that
   the banker review writes to (the free-tier read is where a banker would pin a name).
2. **A volume question for climate founders outside the exchange fork.** The exchange fork asks for
   throughput and its unit (levelten routes there); a carbon or energy company on the marketplace or
   software fork is never asked. Daniil, 6 Sep: "for these climate-related companies we need to ask
   for a volume metric (e.g. the CO2, like for Xpansiv)". Lives in `selector/quiz_fork.py`.
3. **levelten's private lane**, for the bulk pass: a peer like Xpansiv with a revenue figure, or
   Xpansiv's own revenue at the July 2022 round (the row prices on tonnes only). Daniil's note, 6 Sep.
4. **Geography.** No country or region field exists on a private row, so Finn (Germany) and Moove
   (Africa and emerging markets) cannot be told apart. A `country` column on both private files and
   a caveat in `selector/honesty.py` when founder and comparable differ. Post-pilot.
5. **Whatnot cannot price until a GMV reading exists** (rule B8's wiring): all four rounds are GMV
   only. The marketplace fork already asks for GMV; `bases_for` does not offer it. With that and the
   allow-list, tash's private lane is Whatnot 2.5x, StockX 2.11x on GMV.
6. **The relevance gate reads its word score off a one-decimal string** (`_tag_points` parses
   "tags 0.0"), so since the regeneration a shared word worth under 0.05 points counts as no word.
   That is why The Zebra now reaches levelten only through the recorded fallback. Harmless today,
   undocumented, and it should be made explicit or removed. `selector/match_reference.py`.
7. ~~**Tecsys's industry tag** and the near-duplicate industry labels: fold before the taxonomy rule
   is built.~~ **DONE 9 Sep** (`tools/apply_industry_folds_8sep.py`, 10 rows, 204 in 204 out;
   florin and princeps to Insurance). Tecsys keeps Healthcare on purpose (hospital supply chain is
   its market; scheduling-wizard reaches it that way) and its ARCHETYPE moved to the new
   supply-chain software label instead, which was the wrong half.
8. **The lender top-up counts a gross-revenue row as a price** (mondu: Kriya's 0.6x gross held the
   lane at "two priced" while the ARR range refused it). A correct fix counts two on the SAME reading;
   the simple version measured on 6 Sep made numida, kita and tienda-pago worse. Write-up, 3c.
10. **The bulk pass, investor side:** an Accel Atoms deal with a source, so the split card can
    render for an Indian founder; a source check on every house whose band was inherited from a
    programme rather than the house; and unifold's sourcing brief, which the 5-Sep prompt named
    among the four hardest gaps without writing its row.
11. **An Aerospace & Defence end market** does not exist in the industry vocabulary (ultrasonium's
    customers). Add it in the taxonomy build, with Data Centres, Robotics and Pet Care considered
    alongside. March 1 (8 Sep) added three names behind it: orca-aerospace, constellation-space and
    zymbly all pass on names a banker would strike, and ornadyne fails; hop-aero is a fourth
    (9 Sep), and Design & Engineering wants the Route A treatment (CAD and EDA vendors apart from
    design tools and robotics platforms).
9. **Build the taxonomy rule after the pilot** (ruled yes 6 Sep; measured at 29 of 102, gate 95 to
   94; the measurement patch is `TAXONOMY` in `tools/measure_matching_variants_6sep.py` and is the
   build). Fold the near-duplicate industry labels (item 7) first, and add Wealth & Capital Markets
   Platform and Commerce Enablement & Fulfilment to the catch-all list. The re-tags of the morning
   are applied (Owner, AuditBoard, Wix by `tools/apply_retags_6sep.py`) except ultrasonium's end
   market, which is Daniil's to rule (rulings list, item 2).

## TO COMMIT (rule D14)

Anything decided or received that is not yet in a commit lives here, with the file it lives in, and
comes off the list when it is pushed. An item here is unfinished work.

*As of 00:30 UK, 9 September: origin is at `41d64fa`. The working tree holds ONE batch, written
over the bridge with no git run: the evaluator batch (six fixture re-tags, the supply-chain software
archetype, generic words out of the closeness label, the end-market folds, the profiler rules, gate
rule 4, rules A13 to A16, 130 rebaselined golden files, the fixture table regenerated, four new
documents and this one). The suite was run in a fresh clone of `41d64fa` holding the batch before
this block was written: twenty checks, all pass, gate 130 of 142, golden 0 of 142 against the
rebaselined files. Paste this whole block; it stages, commits and pushes, and Vercel deploys from
`main` (nothing on the landing changes):*

```
cd ~/fairway-valuation && rm -f .git/index.lock && git add -A && git commit -m "The evaluator's verdicts applied: six test companies re-tagged, a supply-chain software archetype, generic words out of the closeness label, the end-market labels folded, four profiler rules. Twenty checks, all green; gate 130 of 142; golden rebaselined 130 of 142, every move attributed." -m "Daniil's answers of 8 Sep on the evaluator's 142 verdicts (docs/evaluator-verdicts-review-8sep.md). Fundraisly on the investor-platform angle, alloovium to Construction and Infrastructure, kita software first, lambda-robotics without the data-centre secondary, supercritical as a levelten twin, lyka on the pool's pet label; florin and princeps to Insurance. Route A: Supply Chain and Logistics Software in the software family (Kinaxis, Manhattan, Tecsys, E2open first; Samsara, Freightos, Full Truck Alliance, Loadsmart, Shiprocket second), fork route and investor aliases, ekho-labs, bizmark and derya on it, Samsara pinned for ekho-labs. Rule A14: eighteen stopwords, closeness and shared words on non-generic words, weight file 405 in 400 out, gate rule 4 narrowed to catch-all archetypes. TAKEOVER 7: ten end-market folds, 204 in 204 out. Four classification rules in the profiler prompt, check 17 asserts them. Rules A13 to A16. E1 measured at zero moves, E2 measured and rejected. Sourcing list for the bulk pass." -m "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>" -m "Claude-Session: https://claude.ai/code/session_015PefyidDxPGv9btZdFAqLu" && git push
```

*Success on screen: a `[main <sha>]` line with "154 files changed", then
`41d64fa..<new sha>  main -> main`. "Everything up-to-date" after the push means the commit did not
happen; paste the screen back. If the file count is very different, another session wrote to the
laptop after 00:30 UK; `git add -A` is the rule, so that is fine, but paste the screen back so it
can be named here.*

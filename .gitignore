.vercel
node_modules
.DS_Store
__pycache__/
*.pyc

# Generated by tools/render_harness.py. Output, never source: it is rebuilt from the
# fixtures on every run, so committing it would put 206 files in every diff.
build/

# The Claude desktop app saves every file sent in the chat into this folder inside the repo
# folder. Output, never source (20 Sep 2026). The fourteen files committed before this line
# stay tracked until Daniil says to take them out.
Claude outputs/
